---
phase: 06-auth
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - SPEC-sales-intel.md
autonomous: true
requirements: [D-15, D-16, D-18, D-19, L-08, SPEC-8, SPEC-10, SPEC-13]

estimate:
  tokens: 16000
  raw_tokens: 8000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "SPEC §8 descreve o algoritmo de hash que o codigo realmente usa: scrypt da stdlib com o formato scrypt$n$r$p$salt$chave, e nao argon2 (D-15)"
    - "SPEC §8 lista as tabelas sessoes e a coluna briefings.owner, que a Fase 6 cria (D-16, D-18)"
    - "SPEC §10 lista todas as rotas da fase com a guarda de cada uma, incluindo quais permanecem publicas"
    - "SPEC §13 lista ADMIN_USERNAME e ADMIN_SENHA sem nenhum valor de exemplo (D-19)"
    - "nenhuma senha, chave ou valor default aparece no documento"
  artifacts:
    - "SPEC-sales-intel.md §8 tabela usuarios com senha_hash em scrypt"
    - "SPEC-sales-intel.md §8 tabela sessoes"
    - "SPEC-sales-intel.md §8 coluna briefings.owner"
    - "SPEC-sales-intel.md §10 inventario de rotas com guarda"
    - "SPEC-sales-intel.md §13 ADMIN_USERNAME e ADMIN_SENHA"
  key_links:
    - "o inventario de §10 e a mesma lista que o teste de inventario de rotas do plano 06-04 trava em codigo — os dois precisam concordar"
    - "as variaveis de §13 sao as mesmas lidas por app/config.py no plano 06-04 e as mesmas escritas em .env.example"
---

<objective>
Fazer a SPEC dizer o que o sistema faz. Tres divergencias entram nesta fase e nenhuma pode ficar
silenciosa: o hash e `hashlib.scrypt` e nao argon2 (D-15, decidida pelo usuario por causa de
L-06), o modelo de dados ganha `sessoes` e `briefings.owner` (D-16, D-18), e o contrato ganha
rotas alem das tres nomeadas em §10.

Purpose: D-15 exige explicitamente esta atualizacao — "atualizar `SPEC-sales-intel.md` §8 para
refletir a decisao, em vez de deixar SPEC e codigo divergentes silenciosamente". Divergencia
documentada e decisao; divergencia silenciosa e bug de especificacao.
Output: SPEC §8, §10 e §13 refletindo a Fase 6.
</objective>

<planner_notes>
**Por que este plano roda na onda 1, em paralelo com o tracer.** Ele nao depende de nenhuma
linha de codigo: escreve o que as decisoes travadas ja determinaram. Toca um arquivo unico
(`SPEC-sales-intel.md`) que nenhum outro plano da fase toca, entao nao ha conflito de escrita.
Escrever a especificacao **antes** tambem transforma §10 num contrato que o teste de inventario
de rotas do plano 06-04 vai conferir contra o codigo real.

**Escopo deliberadamente estreito.** Nao reescrever a SPEC, nao mexer em §15 (que e a fonte do
objetivo da fase) e nao mexer em §17. Somente §8, §10 e §13, mais uma nota de uma linha em §8
sobre `conteudo_hash`, que a SPEC lista mas o codigo nunca implementou (aceite R-10 da Fase 5) —
um leitor novo hoje acredita que a coluna existe.
</planner_notes>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@SPEC-sales-intel.md
@.planning/phases/06-auth/06-CONTEXT.md
@.planning/phases/05-llm/05-SECURITY.md
@app/db.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: SPEC §8 e §13 refletem scrypt, as tabelas novas e as variaveis do bootstrap (D-15, D-16, D-18, D-19)</name>
  <files>SPEC-sales-intel.md</files>
  <read_first>
    - `SPEC-sales-intel.md` §8 "Modelo de dados" (linhas 138-161): a tabela `briefings` e a tabela
      `usuarios`, cuja linha `senha_hash | texto (argon2)` e o alvo direto de D-15.
    - `SPEC-sales-intel.md` §13 "Variaveis de ambiente" (linhas 249-260): o bloco a estender.
    - `.planning/phases/06-auth/06-CONTEXT.md` D-15 (formato exato do hash e parametros), D-16
      (sessao), D-18 (dono do briefing), D-19 (bootstrap por ambiente).
    - `app/db.py:28-42` — `criar_tabelas()`, para que os nomes de coluna na SPEC batam com os
      nomes reais.
    - `.planning/phases/05-llm/05-SECURITY.md` — aceite R-10, que explica por que
      `conteudo_hash` nunca foi implementado.
  </read_first>
  <action>
Edicoes cirurgicas em `SPEC-sales-intel.md`. Nao reescreva secoes inteiras, nao renumere nada e
nao toque em §15 nem em §17.

**§8, tabela `briefings`.** Acrescente uma linha a tabela para a coluna nova da Fase 6:
`owner` | texto, nulo | id do usuario que gerou o briefing; nulo em linhas anteriores a Fase 6.
Acrescente logo abaixo da tabela uma frase registrando a politica de leitura de D-18: o filtro de
visibilidade vive na leitura (vendedor ve as proprias linhas, admin ve todas, linha sem dono e
visivel apenas para admin), e nenhuma migracao reescreve ou remove linha existente.
Acrescente tambem uma nota de uma linha marcando `conteudo_hash` como **especificado e nao
implementado**, com a referencia ao risco aceito R-10 da Fase 5 — hoje o leitor supoe que a
coluna existe.

**§8, tabela `usuarios`.** Troque o tipo da linha `senha_hash` para registrar o algoritmo real e
o formato de armazenamento: texto no formato autodescritivo do scrypt, com o algoritmo, os tres
parametros de custo, o salt e a chave derivada separados por cifrao. Mantenha `id` como uuid,
`papel` com os dois valores de L-08 e `ativo` booleano. Acrescente a linha `criado_em`
(timestamp), que a implementacao grava.
Logo abaixo da tabela, acrescente um paragrafo curto de **divergencia registrada**, nomeando:
que a versao anterior deste documento dizia argon2; que a decisao travada D-15 escolheu
`hashlib.scrypt` da biblioteca padrao; que o motivo e a restricao L-06 de nao instalar pacote
novo antes da entrega, sendo `argon2-cffi` um pacote com extensao C compilada; e que os
parametros ficam gravados dentro da propria string para que um aumento futuro de custo nao
invalide hash ja gravado. Registre tambem que a comparacao e feita em tempo constante.

**§8, tabela `sessoes` (nova).** Acrescente uma terceira tabela, no mesmo formato das outras
duas: `token_hash` (texto, chave primaria — o sha256 do token; o token bruto vive apenas no
cookie), `usuario_id` (texto), `criada_em` (timestamp), `expira_em` (timestamp). Uma frase
abaixo registrando D-16: sessao e token opaco de 32 bytes gerados por CSPRNG guardado no banco,
nao JWT e nao cookie assinado a mao; revogacao e remocao de linha; duracao de 12 horas.

**§13, bloco de variaveis.** Acrescente duas linhas ao bloco existente, **ambas sem valor**:
`ADMIN_USERNAME=` e `ADMIN_SENHA=`. Abaixo do bloco, uma frase registrando D-19: as duas semeiam
o primeiro administrador na subida do processo, nao existe cadastro publico, e o sistema nunca
traz credencial embutida — sem as duas variaveis nenhum administrador e criado.
Nao invente valor de exemplo para nenhuma das duas, e nao mexa nas linhas ja existentes do bloco.
  </action>
  <verify>
    <automated>python -c "
d = open('SPEC-sales-intel.md', encoding='utf-8').read()
assert 'argon2' in d, 'a divergencia precisa ser nomeada, nao apagada'
assert 'scrypt' in d
assert d.count('ADMIN_USERNAME=') == 1 and d.count('ADMIN_SENHA=') == 1
i = d.index('ADMIN_SENHA=')
assert d[i+len('ADMIN_SENHA='):i+len('ADMIN_SENHA=')+1] in ('\n', '\r'), 'ADMIN_SENHA nao pode ter valor'
j = d.index('ADMIN_USERNAME=')
assert d[j+len('ADMIN_USERNAME='):j+len('ADMIN_USERNAME=')+1] in ('\n', '\r'), 'ADMIN_USERNAME nao pode ter valor'
assert 'sessoes' in d and 'token_hash' in d
assert 'owner' in d
assert 'conteudo_hash' in d and 'R-10' in d
assert 'D-15' in d and 'D-16' in d and 'D-18' in d and 'D-19' in d
assert '## 15. Fases de entrega' in d
assert '## 17. Defini' in d
print('ok')" && pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - `SPEC-sales-intel.md` §8 descreve `senha_hash` como scrypt com formato autodescritivo, e a
      palavra `argon2` continua no documento **apenas** dentro do paragrafo de divergencia
      registrada, citando D-15 e L-06 — a divergencia e nomeada, nao apagada.
    - §8 contem uma tabela `sessoes` com as colunas `token_hash`, `usuario_id`, `criada_em`,
      `expira_em`, e a frase que registra D-16.
    - §8 contem a coluna `owner` na tabela `briefings` e a frase de politica de leitura de D-18
      (linha sem dono e visivel apenas para admin; nenhuma linha existente e reescrita ou
      removida).
    - §8 marca `conteudo_hash` como especificado e nao implementado, referenciando R-10.
    - §13 contem exatamente uma ocorrencia de `ADMIN_USERNAME=` e uma de `ADMIN_SENHA=`, **ambas
      seguidas imediatamente de quebra de linha** — nenhum valor, nenhuma senha de exemplo (D-19).
    - As secoes §15 e §17 continuam presentes e inalteradas.
    - `pytest -q` continua verde (nenhum arquivo de codigo foi tocado).
  </acceptance_criteria>
  <done>
    A SPEC descreve o modelo de dados que a Fase 6 realmente constroi, e a divergencia de
    algoritmo de hash esta registrada com o motivo, em vez de existir apenas no historico de
    planejamento.
  </done>
</task>

<task type="auto">
  <name>Task 2: SPEC §10 vira inventario de rotas com a guarda de cada uma (D-17, L-07)</name>
  <files>SPEC-sales-intel.md</files>
  <read_first>
    - `SPEC-sales-intel.md` §10 "Contrato da API" (linhas 182-202), em especial a linha
      "Rotas do bonus 2" que hoje lista apenas tres rotas.
    - `.planning/phases/06-auth/06-CONTEXT.md` D-17 (uma rota sem dependencia declarada e uma
      rota desprotegida) e as perguntas em aberto 2 e 3.
    - `app/main.py` — as rotas que ja existem hoje: `/health`, `POST /api/briefings`,
      `GET /api/historico`, `GET /` e o mount de `/static`.
    - `.planning/phases/05-llm/05-SECURITY.md` — aceites R-01 e R-08, que esta secao passa a
      refletir.
  </read_first>
  <action>
Substitua a linha solta "Rotas do bonus 2" de §10 por uma **tabela de inventario** com tres
colunas: rota (metodo e caminho), quem pode chamar, e a guarda que o servidor aplica. A tabela
cobre **todas** as rotas do sistema, nao so as novas — e o ponto de D-17: rota que nao esta na
tabela e rota que nasceu aberta.

Linhas da tabela, nesta ordem:
- `GET /health` — publico — nenhuma; requisito funcional RF13, devolve apenas booleanos.
- `GET /` — publico — nenhuma; e a propria tela de login.
- `GET /static/*` — publico — nenhuma; ativos da UI, sem dado de negocio.
- `POST /api/auth/login` — publico — limite por IP e por username; unica porta que aceita
  credencial.
- `POST /api/auth/logout` — autenticado — `usuario_atual`.
- `GET /api/auth/me` — autenticado — `usuario_atual`.
- `POST /api/briefings` — autenticado — `usuario_atual` mais o limite por IP ja existente.
- `GET /api/historico` — autenticado — `usuario_atual`; vendedor ve apenas as proprias linhas,
  admin ve todas.
- `GET /api/admin/usuarios` — somente admin — `exigir_admin`.
- `POST /api/admin/usuarios` — somente admin — `exigir_admin`.
- `POST /api/admin/usuarios/{usuario_id}/ativo` — somente admin — `exigir_admin`.

Abaixo da tabela, tres paragrafos curtos:
1. **Regra de D-17 e L-07.** A autorizacao e avaliada no servidor a cada requisicao, por
   dependencia declarada na rota. Esconder um botao na interface nao e controle de acesso.
   Uma rota nova sem linha nesta tabela e um defeito, e existe um teste de inventario que
   quebra quando isso acontece.
2. **Por que `/health` continua publico.** Requisito funcional RF13, devolve somente booleanos e
   nenhum dado de negocio; o risco aceito R-08 da Fase 5 fica renovado nesta fase, nao revogado.
3. **Configuracao nao entra pela interface.** Nenhuma rota le ou grava variavel de configuracao
   do processo; a area de administracao e apenas leitura sobre historico e usuarios, mais criacao
   e ativacao de usuario. Consequencia declarada: o risco aceito R-11 da Fase 5 segue valido
   mesmo com a fronteira operador/usuario passando a existir, porque o administrador do produto
   nao ganhou poder sobre a configuracao do processo.

Acrescente ainda, logo apos a tabela, a resposta de erro padronizada: 401 quando nao ha sessao
valida e 403 quando ha sessao mas o papel nao basta — dois estados distintos, ambos com mensagem
autorada e generica.

Nao altere as subsecoes ja existentes de §10 (`POST /api/briefings`, `GET /api/historico`,
`GET /health`) alem de acrescentar a tabela e os paragrafos.
  </action>
  <verify>
    <automated>python -c "
d = open('SPEC-sales-intel.md', encoding='utf-8').read()
for rota in ['/api/auth/login','/api/auth/logout','/api/auth/me','/api/briefings','/api/historico','/api/admin/usuarios','/api/admin/usuarios/{usuario_id}/ativo','/health','/static']:
    assert rota in d, rota
assert 'usuario_atual' in d and 'exigir_admin' in d
assert 'RF13' in d and 'R-08' in d and 'R-11' in d
assert 'D-17' in d and 'L-07' in d
assert '401' in d and '403' in d
print('ok')" && pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - §10 contem uma tabela com as **onze** rotas listadas, cada uma com quem pode chamar e a
      guarda aplicada.
    - As guardas nomeadas na tabela sao exatamente `usuario_atual` e `exigir_admin`, os mesmos
      identificadores que existem em `app/main.py`.
    - A tabela declara explicitamente `GET /health`, `GET /` e `GET /static/*` como publicos, com
      justificativa.
    - §10 registra que 401 e 403 sao estados distintos, com mensagem autorada e generica.
    - §10 registra a renovacao do risco aceito R-08 e a preservacao do risco aceito R-11, este
      ultimo condicionado ao fato de nenhuma rota ler ou gravar configuracao do processo.
    - §10 cita D-17 e L-07 pelo codigo.
    - `pytest -q` continua verde.
  </acceptance_criteria>
  <done>
    §10 deixa de ser uma lista de tres rotas soltas e vira o inventario contra o qual o teste de
    guardas do plano 06-04 pode ser conferido.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repositorio git -> mundo | `SPEC-sales-intel.md` e versionado e publico junto do codigo |
| documento -> leitor futuro | a SPEC e a fonte que orienta quem for mexer no sistema depois |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-18 | Information Disclosure | credencial de exemplo escrita na SPEC | high | mitigate | Task 1: `ADMIN_USERNAME=` e `ADMIN_SENHA=` entram sem valor; criterio de aceite verifica que o caractere seguinte a cada uma e quebra de linha |
| T-06-19 | Repudiation | SPEC e codigo divergindo em silencio sobre o algoritmo de hash | medium | mitigate | Task 1: a divergencia e nomeada num paragrafo proprio citando D-15 e L-06; a palavra do algoritmo antigo permanece no documento **de proposito**, para que a decisao seja rastreavel |
| T-06-20 | Repudiation | coluna especificada e nunca implementada induzindo a erro | low | mitigate | Task 1: `conteudo_hash` marcada como nao implementada com referencia a R-10 |
| T-06-21 | Elevation of Privilege | rota nova nascendo sem guarda porque o contrato nao a exige | high | mitigate | Task 2: §10 vira inventario obrigatorio de rota mais guarda, e o plano 06-04 trava o mesmo inventario em teste automatizado (D-17) |
| T-05-33 | Elevation of Privilege | `/health` sem autenticacao (R-08) | low | **accept — renovado** | RF13; devolve somente booleanos e nenhum dado de negocio; a decisao passa a estar escrita no contrato, nao so no registro de seguranca |
| T-05-41 | Information Disclosure | `LLM_BASE_URL` compondo a URL que carrega o segredo (R-11) | medium | **accept — renovado** | Task 2 declara no contrato que nenhuma rota le ou grava configuracao do processo; a area de administracao nao ganha poder sobre a configuracao, entao a fronteira operador/usuario que passou a existir nesta fase nao alcanca a variavel. A verificacao automatizada dessa promessa esta em `06-04` |

Nenhum pacote novo e instalado neste plano (L-06) — o plano toca apenas um arquivo de
documentacao. O package-legitimacy gate nao se aplica.
</threat_model>

<verification>
- §8 com scrypt, `sessoes`, `briefings.owner` e a nota de `conteudo_hash`.
- §13 com as duas variaveis de bootstrap sem valor.
- §10 com o inventario de onze rotas e as tres notas de politica.
- `pytest -q` verde (nenhum codigo tocado).

</verification>

<success_criteria>
- D-15: a acao explicitamente exigida pela decisao ("atualizar a SPEC §8") esta feita, com o
  motivo registrado.
- D-16, D-18: modelo de dados da fase descrito antes de existir no banco.
- D-17, L-07: §10 passa a exigir guarda declarada por rota.
- D-19: variaveis de bootstrap especificadas sem nenhum valor default.
- Aceites R-08 e R-11 com disposicao explicita no documento, nao apenas no registro de seguranca.
</success_criteria>

## Artifacts this phase produces

**Produzidos por este plano:**
- `SPEC-sales-intel.md` §8 — coluna `briefings.owner`, nota de politica de leitura D-18, nota de
  `conteudo_hash` nao implementado, tabela `usuarios` com `senha_hash` em scrypt e `criado_em`,
  paragrafo de divergencia registrada D-15, tabela `sessoes`
- `SPEC-sales-intel.md` §10 — inventario de onze rotas com guarda, nota de 401 versus 403, nota
  de `/health` publico (R-08), nota de configuracao fora da interface (R-11)
- `SPEC-sales-intel.md` §13 — `ADMIN_USERNAME=` e `ADMIN_SENHA=`

**Demais artefatos da fase (roll-up):**
- Plano 06-01: `app/auth.py` completo (hash scrypt, sessao opaca, CRUD de usuarios), tabelas
  `usuarios` e `sessoes`, `LoginRequest`, `Usuario`, `usuario_atual`, `exigir_admin`, rotas
  `POST /api/auth/login`, `GET /api/auth/me`, `POST /api/auth/logout`,
  `GET /api/admin/usuarios`, limite de login por IP e por username, helper de teste
  `_cliente_autenticado`.
- Plano 06-03: coluna `briefings.owner`, `db.salvar` com dono, `db.listar` fail-closed com
  juncao a esquerda, guardas em `POST /api/briefings` e `GET /api/historico`.
- Plano 06-04: `app/config.py::admin_username`, `::admin_senha`,
  `app/auth.py::semear_admin_inicial`, `::TAM_MINIMO_SENHA`, `::definir_ativo`,
  `::encerrar_sessoes_do_usuario`; `app/schemas.py::CriarUsuarioRequest`,
  `::AlterarAtivoRequest`; `POST /api/admin/usuarios`,
  `POST /api/admin/usuarios/{usuario_id}/ativo`; teste de inventario de rotas
  (`GUARDAS_ESPERADAS`), `.env.example`.
- Plano 06-05: `static/index.html` com `escapar()` cobrindo aspas simples, tela de login, botao
  de sair, painel de historico e area de admin.

<output>
Create `.planning/phases/06-auth/06-02-SUMMARY.md` when done
</output>
