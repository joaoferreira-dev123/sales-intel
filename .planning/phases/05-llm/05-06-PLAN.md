---
phase: 05-llm
plan: 06
type: execute
wave: 5
depends_on: ["05-01", "05-05"]
files_modified:
  - app/schemas.py
  - app/main.py
  - static/index.html
  - test_smoke.py
autonomous: true
requirements: [D-05, D-06, D-07, D-08, D-12]

estimate:
  tokens: 46000
  raw_tokens: 23000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "quando o LLM falha e o heuristico assume, o campo extrator continua heuristico e a degradacao aparece num campo separado de BriefingResponse (D-05)"
    - "o vendedor le o motivo da degradacao em uma frase curta, sem stack trace e sem nada vindo da excecao original quando ela nao e LLMError (D-06)"
    - "o cartao do briefing degradado exibe uma tag de aviso legivel a distancia, num projetor (D-07)"
    - "GET /health continua devolvendo status ok e passa a informar, por booleano, se a chave foi vista (D-08)"
    - "a enumeracao de extrator da SPEC §8 (llm / heuristico / falha) segue intacta na coluna do banco e em /api/historico"
  artifacts:
    - "app/schemas.py::BriefingResponse.degradado"
    - "app/main.py::_extrair_com_fallback (assinatura estendida para 3-tupla)"
    - "app/main.py::health (campo llm_disponivel)"
    - "static/index.html classe CSS .tag-aviso"
  key_links:
    - "BriefingResponse.degradado e opcional e default None, entao o caminho de cache e o caminho feliz continuam validos sem mudanca"
    - "render() em index.html so emite a tag de aviso quando r.degradado existe, e o valor passa por escapar()"
    - "/health le config.llm_api_key(), a mesma fonte usada por escolher_extrator()"
---

<objective>
Tornar a degradacao **visivel para o vendedor**. Sem isso ele le um briefing pobre e conclui que
o produto e ruim, em vez de entender que houve degradacao — que e exatamente o oposto da
mensagem que o desenho da fase quer passar.

Quatro decisoes: o campo separado em `BriefingResponse` (D-05), o motivo curto chegando ao
vendedor (D-06), a tag de aviso no cartao (D-07) e o `/health` informando o modo de operacao
(D-08).

Purpose: item 5 da tabela `<execution_order>` do CONTEXT.md. So rende com o LLM ligado, entao
fica depois da linha de corte.
Output: contrato de API, UI e endpoint de saude refletindo o estado real do extrator.
</objective>

<planner_notes>
**Reversibility (D-05): `costly`.** `degradado` e campo em contrato de API que a UI passa a
consumir. Remove-lo depois exige tocar `app/schemas.py`, `app/main.py` e `static/index.html`
juntos, na mesma mudanca. Nao e `one-way` — nao ha migracao de dados nem coluna nova no banco,
justamente porque D-05 escolheu **nao** poluir a coluna `extrator` — entao nao ha
`checkpoint:decision`. Mas o custo esta registrado na task que implementa a decisao.

**Por que nao usar a coluna `extrator` para sinalizar degradacao.** Seria mais barato marcar
`extrator = "degradado"`, e e exatamente o que D-05 proibe: quebraria a enumeracao da SPEC §8
(`llm` / `heuristico` / `falha`), sujaria a coluna do banco e contaminaria `/api/historico` com
um quarto valor que nenhum consumidor espera. O campo separado custa mais codigo e mantem o
contrato honesto.

**D-08 e extensao aditiva de contrato, registrada.** A SPEC §10 define `/health` como
`{"status": "ok"}`. O acrescimo de `llm_disponivel` nao remove nem renomeia nada, entao nenhum
consumidor existente quebra — mas e uma extensao do contrato especificado, e o CONTEXT.md ja
registra a ressalva. Mantida por valor operacional: permite conferir o modo de operacao minutos
antes de subir no palco, sem gerar briefing e sem gastar chamada de LLM.

**O teste 3 e estendido, nao duplicado.** D-12 fixa **quatro** testes. A assercao do campo
`degradado` entra na funcao `test_extrator_que_falha_nao_derruba_a_requisicao` ja escrita no
plano 03, que naquele momento so podia asseverar status 200 e `extrator == "heuristico"`.
</planner_notes>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@SPEC-sales-intel.md
@.planning/phases/05-llm/05-CONTEXT.md
@.planning/phases/05-llm/05-PATTERNS.md
@app/schemas.py
@app/main.py
@app/extractor.py
@static/index.html
@test_smoke.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Campo de degradacao no contrato e o motivo chegando ao vendedor (D-05, D-06)</name>
  <files>app/schemas.py, app/main.py, test_smoke.py</files>
  <read_first>
    - `app/schemas.py` — `BriefingResponse` (linhas 50-59) como analog de declaracao de campo, e
      `Briefing.segmento` / `Briefing.porte_estimado` (linhas 22-25) como analog de campo
      **opcional** com `Field(default=None, description=...)`.
    - `app/main.py` — estado apos o plano 01: `_extrair_com_fallback` (que devolve 2-tupla) e os
      dois pontos que constroem `BriefingResponse` (caminho de cache e caminho novo).
    - `app/extractor.py` — estado apos o plano 05: `LLMError`, para saber quando e seguro
      interpolar `str(e)` na mensagem mostrada ao vendedor.
    - `test_smoke.py` — a funcao `test_extrator_que_falha_nao_derruba_a_requisicao` do plano 03,
      que sera estendida.
    - `SPEC-sales-intel.md` §8 (a enumeracao `llm` / `heuristico` / `falha` da coluna `extrator`),
      §9 e §10 (falha vira briefing de confianca baixa com o motivo explicado).
  </read_first>
  <action>
**Em `app/schemas.py`** — acrescente um campo opcional a `BriefingResponse`, logo apos
`extrator`, seguindo o mesmo estilo `Field(default=None, description=...)` de
`Briefing.segmento`:

`degradado: str | None = Field(default=None, description="Motivo da degradacao para o
heuristico, quando o extrator principal falhou")`

O campo e opcional e tem default `None`, portanto o caminho de cache e o caminho feliz continuam
construindo `BriefingResponse` sem alteracao. **Nao** toque em `Briefing`, nao mude o tipo de
`extrator` e nao acrescente valor novo a nenhuma enumeracao.

**Em `app/main.py`** — estenda `_extrair_com_fallback` para
`-> tuple[Briefing, str, str | None]`, devolvendo `(briefing, nome_extrator, degradado)`:

- Degrau 1 (extrator escolhido funcionou): terceiro elemento `None`.
- Degrau 2 (extrator falhou, heuristico assumiu): terceiro elemento e a mensagem de D-06. Monte-a
  como `"IA indisponivel, briefing gerado por regras."` seguida, **somente quando a excecao for
  uma `LLMError`**, de um espaco e `str(e)`. Quando for qualquer outra excecao, use apenas a
  frase generica, sem interpolar nada. Motivo: `LLMError` tem mensagens escritas por nos e
  auditadas (plano 05); qualquer outra excecao pode carregar texto arbitrario de terceiro. Trunque
  o resultado final em 200 caracteres. Deixe um comentario inline com esse *porque*.
  Isso exige `from .extractor import HeuristicExtractor, LLMError, escolher_extrator`.
- Degrau 3 (ate o heuristico falhou): `nome_extrator = "falha"` e terceiro elemento `None` — o
  motivo ja vai no `resumo` do briefing de falha, como no braco `except FetchError`.

No corpo de `gerar_briefings`: desempacote os tres valores, mantenha o `db.salvar(...)`
gravando **`nome_extrator`** (que continua `"heuristico"` no caso degradado, por D-05 — a coluna
do banco e `/api/historico` seguem limpos), e passe `degradado=degradado` ao construir o
`BriefingResponse` do caminho novo. No braco `except FetchError`, defina `degradado = None`.
O `BriefingResponse` do caminho de cache **nao** recebe o parametro.

**Em `test_smoke.py`** — estenda a funcao `test_extrator_que_falha_nao_derruba_a_requisicao` ja
existente (nao crie um quinto teste, D-12 fixa quatro). Acrescente tres assercoes ao final:

- `item["degradado"]` e uma string nao vazia que comeca com `IA indisponivel`.
- `item["degradado"]` **nao** contem o texto da excecao levantada pelo extrator falso do teste —
  provando que texto arbitrario de excecao nao vaza para a tela do vendedor.
- `item["extrator"] == "heuristico"` (ja presente) continua valendo — a degradacao **nao** foi
  sinalizada pela coluna `extrator`.
  </action>
  <verify>
    <automated>pytest -q && python -c "
from app.schemas import BriefingResponse
from datetime import datetime, timezone
from app.schemas import Briefing
b = Briefing(empresa='X', resumo='y')
r = BriefingResponse(url='https://x', briefing=b, origem='cache', extrator='llm', coletado_em=datetime.now(timezone.utc))
assert r.degradado is None
import inspect, app.main as m
assert 'tuple[Briefing, str, str | None]' in inspect.getsource(m._extrair_com_fallback)
print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `pytest -q` sai com codigo 0 e reporta **7** testes passando.
    - `BriefingResponse` construido sem o parametro `degradado` continua valido e o campo vale
      `None` (caminho de cache nao quebrou).
    - `python -c "from app.schemas import BriefingResponse; f=BriefingResponse.model_fields['degradado']; assert f.default is None; assert f.annotation == (str | None); print('ok')"` imprime `ok`.
    - `grep -c 'degradado' app/schemas.py` imprime `1`.
    - Em `test_extrator_que_falha_nao_derruba_a_requisicao`, a resposta traz `degradado`
      comecando com `IA indisponivel`, `extrator` igual a `heuristico`, e **nada** do texto da
      excecao levantada pelo extrator falso.
    - `grep -c 'def test_' test_smoke.py` imprime `7` (nenhum teste novo foi criado).
    - `grep -v '^\s*#' app/db.py | grep -c 'degradado' || true` imprime `0` — o campo nao chega ao
      banco (D-05, SPEC §8).
  </acceptance_criteria>
  <done>
    A degradacao viaja num campo proprio do contrato de API, com mensagem curta e auditada; a
    coluna `extrator` e o historico continuam com a enumeracao da SPEC §8.
  </done>
  <reversibility rating="costly">
    D-05: `degradado` e campo de contrato de API ja consumido pela UI. Remove-lo depois exige
    tocar `app/schemas.py`, `app/main.py` e `static/index.html` na mesma mudanca. Nao ha migracao
    de banco envolvida, entao nao e `one-way`.
  </reversibility>
</task>

<task type="auto">
  <name>Task 2: Tag de aviso no cartao do briefing degradado (D-07)</name>
  <files>static/index.html</files>
  <read_first>
    - `static/index.html` — estado apos o plano 02 (UTF-8 sem BOM). Alvos: o bloco `:root` com as
      variaveis de cor (linha 8, `--accent:#e4572e`), a classe `.tag` (linhas 27-28) e a funcao
      `render()` (linhas 89-106), em especial o `<div class="meta">` com as tres tags atuais.
    - `.planning/phases/05-llm/05-PATTERNS.md` secao "`static/index.html`" — D-07 reaproveita a
      classe `.tag` existente com um modificador de cor de atencao.
    - `app/schemas.py` — o nome exato do campo (`degradado`), que precisa bater com `r.degradado`
      no JavaScript.
  </read_first>
  <action>
Torne a degradacao legivel a distancia, num projetor.

**CSS** — acrescente **uma** regra logo abaixo da regra `.tag` existente, reaproveitando a
variavel de cor de destaque ja declarada em `:root`:

`.tag-aviso { background:#fde8e0; color:var(--accent); font-weight:600; }`

Nao redefina `.tag`, nao adicione variaveis novas ao `:root` e nao mude nenhuma outra regra.

**JavaScript** — dentro de `render(r)`, no `<div class="meta">`, acrescente uma quarta
`<span>` **condicional**, imediatamente apos a tag de confianca e antes de `${escapar(r.url)}`.
Ela so e emitida quando `r.degradado` tiver valor, usando o operador ternario ja empregado na
primeira tag do mesmo bloco, e a classe combina a base com o modificador:
`class="tag tag-aviso"`. O conteudo e `${escapar(r.degradado)}` — **obrigatoriamente** passando
por `escapar()`, como todo valor vindo da API.

Quando `r.degradado` for nulo ou ausente, a expressao produz string vazia e o cartao fica
exatamente como esta hoje.

Nao altere `lista()`, `escapar()`, o `fetch` de `/api/briefings`, nem qualquer outro trecho.
Nao reintroduza BOM nem duplo encode — o arquivo continua UTF-8 sem BOM.
  </action>
  <verify>
    <automated>python -c "
d = open('static/index.html', encoding='utf-8').read()
b = open('static/index.html','rb').read()
assert b[:3] != b'\xef\xbb\xbf' and b.count(b'\xc3\x83') == 0
assert '.tag-aviso' in d
assert 'r.degradado' in d and 'escapar(r.degradado)' in d
assert 'tag tag-aviso' in d
i = d.index('escapar(r.degradado)')
assert d.count('escapar(r.degradado)') == 1
assert 'var(--accent)' in d.split('.tag-aviso')[1].split('}')[0]
print('ok')" && pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - `static/index.html` contem a regra CSS `.tag-aviso` e ela usa `var(--accent)`.
    - `render()` emite `<span class="tag tag-aviso">` apenas quando `r.degradado` tem valor
      (a expressao e condicional, nao incondicional).
    - O valor exibido passa por `escapar(r.degradado)` — exatamente uma ocorrencia.
    - O arquivo continua sem BOM e sem nenhum byte `C3 83` (o plano 02 nao regrediu).
    - `grep -c ':root' static/index.html` imprime `1` (nenhum bloco de variaveis novo).
    - `pytest -q` sai com codigo 0 e reporta 7 testes.
  </acceptance_criteria>
  <done>
    Um briefing degradado se distingue visualmente de um briefing normal no cartao, com custo de
    ~6 linhas e nenhuma classe CSS nova alem do modificador.
  </done>
</task>

<task type="auto">
  <name>Task 3: /health informa qual extrator esta ativo (D-08)</name>
  <files>app/main.py</files>
  <read_first>
    - `app/main.py` — a rota `health()` (linhas 33-35, `return {"status": "ok"}`) e o bloco de
      imports locais.
    - `app/config.py` — `llm_api_key()`, a mesma fonte que `escolher_extrator()` consulta.
    - `SPEC-sales-intel.md` §10 — o contrato declarado de `/health`, que esta sendo estendido de
      forma aditiva.
    - `.planning/phases/05-llm/05-CONTEXT.md` D-08 — inclui a ressalva registrada sobre a
      extensao de contrato.
  </read_first>
  <action>
Estenda `GET /health` para responder tambem qual e o modo de operacao, permitindo conferir o
estado minutos antes da apresentacao sem gerar briefing e sem gastar chamada de LLM.

Acrescente `from . import config` ao bloco de imports locais de `app/main.py` (junto de
`from . import db`).

A rota passa a devolver `{"status": "ok", "llm_disponivel": bool(config.llm_api_key())}`.

Restricoes duras:
- A chave `"status"` continua existindo com o valor `"ok"` — a mudanca e **aditiva** e nenhum
  consumidor existente pode quebrar (SPEC §10).
- O novo campo e **booleano**. Nunca devolva a chave, um prefixo dela, o comprimento dela, nem o
  nome do modelo configurado. O endpoint e publico e nao tem autenticacao nesta fase.
- Use `config.llm_api_key()` e nao `os.getenv` — a fonte precisa ser a mesma consultada por
  `escolher_extrator()`, senao `/health` pode mentir sobre o modo de operacao.
- Deixe um comentario de uma linha registrando que este campo e extensao aditiva ao contrato da
  SPEC §10, conforme D-08.
  </action>
  <verify>
    <automated>pytest -q && python -c "
import os
os.environ.pop('LLM_API_KEY', None)
from fastapi.testclient import TestClient
import app.main as m
c = TestClient(m.app)
r = c.get('/health'); j = r.json()
assert r.status_code == 200 and j['status'] == 'ok'
assert j['llm_disponivel'] is False, j
assert set(j) == {'status', 'llm_disponivel'}, j
os.environ['LLM_API_KEY'] = 'chave-de-teste-sem-valor'
j2 = c.get('/health').json()
assert j2['llm_disponivel'] is True
assert 'chave-de-teste-sem-valor' not in c.get('/health').text
print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `GET /health` devolve 200 com `status` igual a `ok` — contrato da SPEC §10 preservado.
    - A resposta tem exatamente duas chaves: `status` e `llm_disponivel`.
    - `llm_disponivel` e `false` sem `LLM_API_KEY` no ambiente e `true` com ela definida, lido em
      tempo de chamada (nao congelado no import).
    - O corpo da resposta **nunca** contem o valor da chave, nem parte dele, nem o nome do modelo.
    - `grep -v '^\s*#' app/main.py | grep -c 'os.getenv' || true` imprime `0`.
    - `pytest -q` sai com codigo 0 e reporta 7 testes.
  </acceptance_criteria>
  <done>
    Da para conferir o modo de operacao com um `curl /health` antes de subir no palco, sem gerar
    briefing e sem expor segredo.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API -> navegador | o novo campo `degradado` cruza aqui e vai parar no DOM |
| excecao interna -> texto mostrado ao vendedor | fronteira onde texto arbitrario poderia vazar |
| `/health` -> qualquer cliente nao autenticado | endpoint publico expondo estado de configuracao |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-30 | Information Disclosure | mensagem de degradacao mostrada ao vendedor | high | mitigate | Task 1 so interpola `str(e)` quando a excecao e `LLMError` (mensagens escritas e auditadas no plano 05); qualquer outra excecao produz apenas a frase generica; truncagem em 200 caracteres; teste 3 assevera que o texto da excecao original nao aparece |
| T-05-31 | Cross-Site Scripting | `r.degradado` renderizado no cartao | medium | mitigate | Task 2 obriga `escapar(r.degradado)`, mesma defesa aplicada a todo valor vindo da API; criterio de aceite conta a ocorrencia |
| T-05-32 | Information Disclosure | `/health` publico expondo estado de configuracao | medium | mitigate | Task 3 devolve **apenas booleano**; proibido devolver a chave, prefixo, comprimento ou nome do modelo; criterio de aceite fixa o conjunto de chaves da resposta |
| T-05-33 | Elevation of Privilege | `/health` sem autenticacao | low | accept | endpoint de verificacao de saude e requisito funcional RF13; nao expoe dado de negocio; autenticacao e escopo da Fase 6 |
| T-05-34 | Tampering | poluicao da enumeracao `extrator` da SPEC §8 | medium | mitigate | D-05 mantem `extrator` em `heuristico`; criterio de aceite verifica que `degradado` nao aparece em `app/db.py` e portanto nao chega ao banco nem a `/api/historico` |
| T-05-35 | Repudiation | degradacao silenciosa levando o vendedor a culpar o produto | high | mitigate | e o proposito do plano inteiro (D-06, D-07): a degradacao deixa de ser silenciosa e vira tag visivel no cartao |

Nenhum pacote novo e instalado neste plano (L-06); o package-legitimacy gate nao se aplica.
</threat_model>

<verification>
- `pytest -q` verde com 7 testes, incluindo o teste 3 estendido.
- `GET /health` aditivo e sem segredo.
- Cartao degradado com tag de aviso, valor escapado.
</verification>

<success_criteria>
- D-05: degradacao em campo proprio; `extrator` permanece `heuristico`; SPEC §8 intacta.
- D-06: motivo curto, sem stack trace e sem texto arbitrario de excecao.
- D-07: tag de aviso reaproveitando `.tag`, com cor de atencao.
- D-08: `/health` aditivo, booleano, lendo a mesma fonte de `escolher_extrator()`.
- D-12: continuam sendo quatro testes; o teste 3 foi estendido, nao duplicado.
</success_criteria>

## Artifacts this phase produces

**Produzidos por este plano:**
- `app/schemas.py::BriefingResponse.degradado: str | None`
- `app/main.py::_extrair_com_fallback` com assinatura `-> tuple[Briefing, str, str | None]`
- `app/main.py::health` com o campo `llm_disponivel`
- `static/index.html` classe CSS `.tag-aviso` e a `<span class="tag tag-aviso">` condicional em
  `render()`
- `test_smoke.py::test_extrator_que_falha_nao_derruba_a_requisicao` estendido com as assercoes do
  campo `degradado`

**Demais artefatos da fase (roll-up completo):**
- Plano 01: `app/main.py::_extrair_com_fallback`, `::BASE_DIR`, `::STATIC_DIR`.
- Plano 02: `app/fetcher.py::ROBOTS_TIMEOUT`; `pode_raspar()` reescrito; `static/index.html` em
  UTF-8 sem BOM.
- Plano 03: os tres testes de fallback em `test_smoke.py`.
- Plano 04: `app/config.py` com `llm_api_key()`, `LLM_MODELO`, `LLM_MAX_CHARS` e `LLM_BASE_URL`
  (D-13); `.env.example` com as quatro variaveis.
- Plano 05: `app/extractor.py::LLMError`, `::_SemJsonSchema`, `::LLM_TIMEOUT`,
  `::DELIM_INICIO`, `::DELIM_FIM`, `::LLMExtractor._montar_mensagens`,
  `::LLMExtractor._chamar_provedor` (que le `app/config.py::LLM_BASE_URL`),
  `::LLMExtractor.extrair`; `test_smoke.py::test_llm_sem_chave_levanta_erro_claro`;
  verificacao contra modelo real (D-14).
- Plano 07: `app/db.py::buscar(url, llm_disponivel)` com a regra de upgrade de extrator.

<output>
Create `.planning/phases/05-llm/05-06-SUMMARY.md` when done
</output>
