---
phase: 05-llm
plan: 08
type: execute
wave: 1
depends_on: []
gap_closure: true
files_modified:
  - app/main.py
  - test_smoke.py
autonomous: true
requirements: [L-02, L-06, D-01, D-05, D-06, D-10, D-12]

estimate:
  tokens: 48000
  raw_tokens: 24000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "uma falha de gravacao no cache degrada apenas a URL afetada: POST /api/briefings responde 200, a URL afetada ainda traz o briefing que ja tinha sido gerado, e as demais URLs do mesmo lote seguem intactas (L-02)"
    - "uma linha de cache incompativel com o schema atual de Briefing vira miss de cache: a URL e recoletada e responde com origem novo, em vez de propagar ValidationError ate o chamador (L-02)"
    - "nenhuma excecao levantada dentro do corpo por URL de gerar_briefings escapa para o chamador: qualquer erro inesperado vira item de falha daquela URL, e o lote continua (L-02)"
    - "a mensagem de degradacao continua curta, sem stack trace e sem texto de excecao nao autorada, truncada em 200 caracteres (D-05, D-06)"
    - "pytest -q roda offline, sem chave de API e sem rede, com 10 testes verdes (D-12)"
  artifacts:
    - "app/main.py::AVISO_CACHE_INDISPONIVEL"
    - "app/main.py::MSG_FALHA_GENERICA"
    - "test_smoke.py::test_falha_ao_salvar_no_cache_nao_derruba_o_lote"
    - "test_smoke.py::test_cache_incompativel_com_o_schema_vira_miss"
    - "test_smoke.py::test_falha_dupla_devolve_briefing_de_falha_sem_vazar_excecao"
  key_links:
    - "o corpo por URL de gerar_briefings (busca de cache, fetch, extracao, gravacao) esta inteiro dentro de um unico try, com bracos except FetchError e except Exception"
    - "db.salvar e chamado dentro de um try aninhado que preserva o briefing ja gerado e anota a degradacao em BriefingResponse.degradado"
    - "a construcao de BriefingResponse a partir da linha de cache esta dentro de um try aninhado que converte (ValidationError, TypeError) em miss, caindo para a extracao fresca"
    - "o degrau 3 de _extrair_com_fallback usa MSG_FALHA_GENERICA em vez de interpolar a excecao do heuristico (D-06)"
---

<objective>
Fechar o único blocker da Fase 5: **L-02 está violada em `app/main.py::gerar_briefings`**.

O laço por URL guarda apenas `except FetchError`. Duas rotas de exceção escapam desse guarda
e derrubam o lote inteiro — a `05-VERIFICATION.md` reproduziu as duas com `TestClient` real:

1. `db.salvar(...)` (main.py:125) — uma falha de escrita propaga sem tratamento. Na
   reprodução, isso destruiu junto uma **segunda URL do mesmo lote que já tinha sido
   processada com sucesso**. É exatamente o cenário que L-02 proíbe.
2. `Briefing(**dados)` no caminho de leitura do cache (main.py:110) — uma linha gravada por
   uma versão anterior do schema levanta `ValidationError` sem tratamento.

O próprio docstring de `gerar_briefings` (main.py:92-94) já declara a garantia que está sendo
quebrada: *"Um link que falha nao derruba os outros: cada URL e tratada de forma independente"*.

Este plano transforma essa garantia de **prosa em invariante estrutural**: todo o corpo por URL
passa a viver dentro de um `try` com braço `except Exception`, e as duas rotas nomeadas ganham
tratamento específico (o briefing sobrevive à falha de gravação; a linha de cache incompatível
vira miss). As duas reproduções da verificação viram **testes offline persistidos**, para que a
lacuna não possa reabrir em silêncio.

Purpose: fechar o gap `L-02` de `05-VERIFICATION.md` / `CR-01` de `05-REVIEW.md`. É o único item
que impede a fase de selar.
Output: `app/main.py` com degradação por URL estrutural; `test_smoke.py` com 3 testes novos
(7 → 10), todos sem chave e sem rede.
</objective>

<planner_notes>
**Tracer-first, aplicado ao formato de fechamento de lacuna.** `TRACER_MODE=true`. A Task 1 é a
fatia vertical: ela atravessa rota HTTP → busca de cache → fetch → extração → gravação →
resposta, num único caminho real, e é verificada de ponta a ponta com `TestClient` sobre um lote
de 2 URLs. As Tasks 2 e 3 expandem a partir dessa fatia provada — refinam rotas específicas
dentro da estrutura que a Task 1 estabeleceu. Cada task é um ciclo vermelho→verde completo: o
teste que reproduz a falha entra antes da correção.

**Ordem deliberada entre Task 1 e Task 2.** Depois da Task 1, a linha de cache incompatível já
**não derruba mais o lote** — o braço `except Exception` a converte em item de falha, e L-02
passa a valer estruturalmente. A Task 2 então **eleva** aquela rota específica de "item de falha"
para "miss de cache com recoleta", que é a forma que a `05-VERIFICATION.md` pede nominalmente
(*"item de falha ou miss de cache"*). Isso é intencional, não retrabalho: a garantia estrutural
chega na primeira task, o comportamento de produto correto chega na segunda, e o teste da Task 2
falha de forma diferente (e legível) depois da Task 1 — `extrator: falha` em vez de
`origem: novo` —, o que prova que as duas camadas são distintas.

**Uma terceira rota, não nomeada pela verificação, entra junto.** `db.buscar()` também pode
levantar — `json.loads` sobre um `briefing` corrompido (db.py:63), `datetime.fromisoformat` sobre
um `coletado_em` inválido (db.py:56), ou erro de leitura do SQLite. A verificação nomeou duas
rotas porque foram as duas que ela reproduziu; L-02 é uma garantia sobre **qualquer** exceção. Por
isso o `try` sobe para cobrir o corpo por URL inteiro em vez de ganhar remendos pontuais. Cobrir a
classe em vez dos dois casos é o que impede o mesmo gap de reabrir por uma quarta rota.

**WR-03 entra neste plano (Task 3), e por quê.** É um defeito de ~3 linhas no **mesmo arquivo,
sob o mesmo contrato D-06** que as Tasks 1 e 2 preservam: o degrau 3 de `_extrair_com_fallback`
interpola `str()` de uma exceção não autorada direto num campo que o vendedor lê. Manter a regra
"só interpolamos mensagem que nós escrevemos" consistente nos três pontos do arquivo é mais barato
agora, com o arquivo já aberto, do que num plano separado. Como bônus, o degrau 3 — hoje sem
nenhuma cobertura — ganha o primeiro teste. **WR-01 e WR-02 ficam no plano 05-09**, deliberadamente
separáveis: se a batelada precisar encolher, o blocker sela sozinho nesta onda.

**Probe spec-less: pulado, visivelmente.** Não existe `05-SPEC.md` nem `REQUIREMENTS.md` nesta
fase; `phase_req_ids` é `null`. *Spec-less probe fallback skipped: phase has no requirement IDs to
probe (visible skip).* Nenhum predicado de borda ou proibição derivado de probe foi gerado nesta
rodada. Os `must_haves` vêm do bloco `gaps:` de `05-VERIFICATION.md` e das decisões travadas
`L-02`, `D-05`, `D-06`, `D-10`, `D-12` do `05-CONTEXT.md`.

**Nyquist / Dimensão 8:** `05-VALIDATION.md` não existe — o modo `--gaps` pula a pesquisa por
desenho, e o usuário optou explicitamente por seguir sem ele. Registrado aqui como ausência
conhecida, não suprido com estratégia inventada. Toda task deste plano tem `<verify><automated>`
executável.

**api-coverage: sem mudança.** `COVERAGE.md` já existe para esta fase e permanece **correto e
intocado**. Esta batelada não toca a superfície da API externa: a correção é tratamento local de
exceção em torno do SQLite e do Pydantic, dentro do processo. Nenhuma capacidade do Groq entra ou
sai, nenhuma linha da matriz muda. Decisão registrada em vez de matriz regerada.

**assumption-delta: `no-change`.** O detector depende de `ROADMAP.md`, que não existe — a query
devolve `detected: false` e o checkpoint não dispara. Confirmado também pela leitura do escopo:
nenhuma transição singular→plural, obrigatório→opcional ou derivado→escolhido nesta batelada. O
substantivo primário continua sendo a **URL** (a unidade de degradação), e este plano justamente
**reforça** essa identidade em vez de generalizá-la.

**schema-gate: não se aplica.** Nenhum padrão de ORM detectado (Prisma, Drizzle, Payload,
Supabase, TypeORM) — o projeto fala SQLite via `sqlite3` direto. Não há comando de push de schema
a injetar, e **D-10 proíbe qualquer migração ou DDL destrutiva** nesta fase. Nenhuma task deste
plano executa `ALTER`, `DROP` ou `DELETE`.

**Reversibilidade:** as três tasks são `reversible` — todas as mudanças estão contidas em uma
função e duas constantes de módulo de `app/main.py`, mais testes aditivos. Nenhum contrato de API
muda de forma, nenhum dado em disco é reescrito. `REVERSIBILITY_GATES=true` está ligado, e
**nenhum `checkpoint:decision` é justificado aqui** — inventar um seria manufaturar um portão que
o risco real não pede.
</planner_notes>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@SPEC-sales-intel.md
@.planning/phases/05-llm/05-CONTEXT.md
@.planning/phases/05-llm/05-VERIFICATION.md
@.planning/phases/05-llm/05-REVIEW.md
@.planning/phases/05-llm/05-PATTERNS.md
@app/main.py
@app/db.py
@app/schemas.py
@test_smoke.py
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Falha de gravacao degrada so a URL afetada, e nenhuma excecao escapa do laco</name>
  <files>app/main.py, test_smoke.py</files>
  <read_first>
    - `app/main.py` — **arquivo sendo modificado, leia o estado atual inteiro**. Pontos que
      importam: o laço `for url_obj in req.urls` (linhas 98-147), o bloco de cache antes do `try`
      (linhas 101-116), o `try` atual (linha 118), a chamada `db.salvar` (linha 125), o braço
      `except FetchError as e` (linhas 127-136) — que é o analog exato do formato "falha vira
      resultado" — e o `resultados.append(BriefingResponse(...))` compartilhado (linhas 138-147).
    - `app/db.py` — `salvar()` (linhas 66-80) e `buscar()` (linhas 41-63). Note que `buscar()`
      também pode levantar: `json.loads` na linha 63 e `datetime.fromisoformat` na linha 56.
    - `app/schemas.py` — `BriefingResponse` (linhas 50-63), em especial o campo `degradado`
      (linhas 59-62), que é onde a degradação de gravação vai viajar.
    - `test_smoke.py` — **arquivo sendo modificado**. O teste
      `test_extrator_que_falha_nao_derruba_a_requisicao` (linhas 48-78) é o **molde exato** a
      replicar: quais símbolos são monkeypatchados em qual módulo, e o truque do `TestClient`
      sem `with`.
    - `.planning/phases/05-llm/05-VERIFICATION.md` — seção "Behavioral Spot-Checks", as duas
      linhas marcadas `✗ FAIL`: são as reproduções que este plano precisa persistir.
    - `.planning/phases/05-llm/05-REVIEW.md` — `CR-01`, que traz o formato de correção sugerido.
  </read_first>
  <behavior>
    - Lote de 2 URLs, `db.salvar` levantando exceção **apenas na primeira**: a resposta é HTTP
      200 com 2 itens. A segunda URL volta com seu briefing normal, `origem: novo`,
      `extrator: heuristico` — não é perdida junto.
    - A primeira URL **também** volta com briefing: a extração já tinha dado certo, e falhar em
      gravar no cache é falha de otimização, não motivo para descartar trabalho já feito. Ela
      volta com `extrator: heuristico` e o campo `degradado` preenchido com o aviso de cache.
    - O texto da exceção de gravação **não aparece** em nenhum campo da resposta: o campo
      `degradado` é exatamente o literal autorado, sem sufixo.
    - Qualquer outra exceção inesperada dentro do corpo por URL vira item de falha daquela URL
      (`extrator: falha`, `confianca: baixa`), e o laço continua para as URLs seguintes.
  </behavior>
  <action>
Trabalhe em duas metades: primeiro o teste que reproduz a falha (deve ficar **vermelho** contra o
código atual), depois a correção que o deixa verde.

**Metade 1 — teste de regressão em `test_smoke.py`.**

Acrescente `test_falha_ao_salvar_no_cache_nao_derruba_o_lote(monkeypatch)` no fim do arquivo,
seguindo a forma de `test_extrator_que_falha_nao_derruba_a_requisicao`: função `test_*` solta no
módulo, sem classe e sem fixture, `assert` direto.

Detalhes que decidem se o teste funciona:

- Monkeypatche **em quais módulos**: `main.buscar_html` e `main.escolher_extrator` (são símbolos
  importados para dentro do namespace de `main`), mas `db.salvar` e `db.buscar` (são acessados
  como atributo de módulo por `main`). O teste existente já faz exatamente essa distinção — copie-a.
- `monkeypatch.delenv("LLM_API_KEY", raising=False)` é **obrigatório**: sem isso,
  `escolher_extrator()` devolve `LLMExtractor` na máquina de quem tem a chave exportada e o teste
  abre socket, violando D-12. Deixe o extrator real rodar (o heurístico) — não monkeypatche
  `escolher_extrator` aqui; o alvo do teste é a gravação, não a extração.
- `db.buscar` devolve sempre `None` (evita tocar `briefings.db`).
- `main.buscar_html` devolve a constante `HTML` já existente no arquivo, para qualquer URL.
- `db.salvar` vira uma função nomeada (não `lambda`, porque precisa ramificar): levanta
  `RuntimeError` quando o primeiro argumento contém o segmento `quebra`; caso contrário devolve
  `datetime.now(timezone.utc)`.
- As duas URLs do corpo são `https://acme.com.br/quebra` e `https://acme.com.br/boa`, **nessa
  ordem**. Use caminhos explícitos: `HttpUrl` acrescenta barra final a domínio sem caminho, e
  URLs com caminho atravessam a normalização sem surpresa — o que mantém a asserção sobre
  `item["url"]` estável.
- `forcar_atualizacao: true` no corpo da requisição.
- `TestClient(main.app)` **sem** `with`, para não disparar o lifespan nem criar `briefings.db` —
  o teste existente documenta esse motivo num comentário; mantenha um comentário equivalente.

Asserções: status 200; `len(dados) == 2`; o item 1 traz `extrator == "heuristico"`,
`origem == "novo"` e `briefing["empresa"] == "Acme Tecnologia"` (prova que a URL boa sobreviveu);
o item 0 traz `extrator == "heuristico"`, `briefing["empresa"] == "Acme Tecnologia"` (prova que o
briefing já gerado foi preservado) e `degradado == main.AVISO_CACHE_INDISPONIVEL` por **igualdade
exata** — a igualdade exata é o que prova que nenhum texto de exceção vazou para o campo, sem
precisar de asserção negativa.

Rode `pytest -q` e confirme que o teste novo **falha** antes de aplicar a metade 2.

**Metade 2 — correção em `app/main.py`.**

*(a) Duas constantes de módulo,* declaradas logo abaixo de `STATIC_DIR` (linha 31), em
UPPER_SNAKE_CASE, com o mesmo estilo ASCII sem acento das demais strings do arquivo:

- `AVISO_CACHE_INDISPONIVEL` com o valor `Briefing gerado, mas nao foi possivel gravar no cache.`
- `MSG_FALHA_GENERICA` com o valor `Nao foi possivel gerar o briefing para este link.`

`MSG_FALHA_GENERICA` é usada pelo braço novo desta task e reaproveitada pela Task 3 — declare-a
agora, uma vez só.

*(b) Subir o `try` para cobrir o corpo por URL inteiro.* Hoje o bloco de cache
(`if not req.forcar_atualizacao:` ... `continue`) fica **antes** do `try`. Mova o `try:` para
logo depois de `url = str(url_obj)`, de modo que a busca de cache, o fetch, a extração e a
gravação fiquem todos dentro dele. O `continue` do caminho de cache funciona normalmente de
dentro de um `try` — não precisa de tratamento especial. Não altere a lógica de cache nesta task;
ela é o alvo da Task 2.

*(c) Guarda específica em torno de `db.salvar`.* Dentro do ramo `else` (o caso
`nome_extrator != "falha"`), envolva a chamada `db.salvar(url, briefing.model_dump(),
nome_extrator)` num `try` aninhado. No `except Exception`: atribua
`coletado_em = datetime.now(timezone.utc)` e anexe o aviso ao campo de degradação pela regra —
quando `degradado` for `None`, passa a valer `AVISO_CACHE_INDISPONIVEL`; quando já tiver valor
(o LLM falhou e caiu no heurístico antes), concatene os dois separados por espaço e trunque em
200 caracteres, que é o mesmo teto que `_extrair_com_fallback` já aplica (D-06). **Não** interpole
`str()` da exceção: uma falha de SQLite não é mensagem autorada por nós, e D-06 restringe a
interpolação às mensagens que escrevemos. Deixe um comentário inline curto explicando essa escolha,
no tom dos comentários já presentes no arquivo.

*(d) Braço `except Exception` no `try` externo,* declarado **depois** de
`except FetchError as e:` (a ordem importa: o mais específico primeiro). Ele não vincula a
exceção a um nome — não há nada de seguro a fazer com ela num campo que o vendedor lê. O corpo
espelha o braço do `FetchError`: `briefing = Briefing(empresa=url, resumo=MSG_FALHA_GENERICA,
confianca="baixa")`, `coletado_em = datetime.now(timezone.utc)`, `origem = "novo"`,
`nome_extrator = "falha"`, `degradado = None`. Deixe um comentário inline registrando que este
braço é a garantia estrutural de L-02: cobre a **classe** de falhas fora do fetch e do extrator
(gravação, leitura de cache, desserialização), não apenas as duas rotas já conhecidas.

*(e)* Mantenha o `resultados.append(BriefingResponse(...))` compartilhado onde está, **depois** do
`try/except`. Os dois braços continuam apenas atribuindo os locais e caindo nele.

Não toque em `app/db.py`, `app/schemas.py`, `app/extractor.py` nem em `requirements.txt` (L-06,
D-01). Nenhuma DDL, nenhuma escrita corretiva no banco (D-10).
  </action>
  <verify>
    <automated>pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest -q` sai com código 0 e reporta **8 testes passando** (7 existentes + 1 novo).
      Equivalente com o interpretador do projeto: `.venv/Scripts/python.exe -m pytest -q`.
    - `pytest -q -k falha_ao_salvar` sai com código 0 e reporta 1 teste passando.
    - Comportamento (travado pelo teste novo): `POST /api/briefings` com as 2 URLs e `db.salvar`
      levantando `RuntimeError` na primeira devolve **200** com 2 itens; o item de índice 1 traz
      `extrator == "heuristico"`, `origem == "novo"` e `briefing.empresa == "Acme Tecnologia"`.
    - Comportamento: o item de índice 0 traz `briefing.empresa == "Acme Tecnologia"` e
      `degradado` **exatamente igual** a `main.AVISO_CACHE_INDISPONIVEL`.
    - `python -c "import app.main as m; assert m.AVISO_CACHE_INDISPONIVEL and m.MSG_FALHA_GENERICA; assert len(m.AVISO_CACHE_INDISPONIVEL) < 200 and len(m.MSG_FALHA_GENERICA) < 200; print('ok')"` imprime `ok`.
    - `python -c "import ast; t=ast.parse(open('app/main.py',encoding='utf-8').read()); fn=[n for n in ast.walk(t) if isinstance(n,ast.FunctionDef) and n.name=='gerar_briefings'][0]; hs=[h for n in ast.walk(fn) if isinstance(n,ast.Try) for h in n.handlers]; assert any(isinstance(h.type,ast.Name) and h.type.id=='Exception' and h.name is None for h in hs), 'sem braco catch-all sem nome'; print('ok')"` imprime `ok`.
    - `python -c "import ast; t=ast.parse(open('app/main.py',encoding='utf-8').read()); fn=[n for n in ast.walk(t) if isinstance(n,ast.FunctionDef) and n.name=='gerar_briefings'][0]; loop=[n for n in ast.walk(fn) if isinstance(n,ast.For)][0]; assert sum(1 for n in ast.walk(loop) if isinstance(n,ast.Try))>=2, 'try aninhado da gravacao ausente'; print('ok')"` imprime `ok`.
    - `git diff --stat -- requirements.txt` não imprime nada (L-06, D-01).
    - `git diff -- app/db.py app/schemas.py app/extractor.py` não imprime nada.
    - Nenhum teste do arquivo abre socket nem lê `LLM_API_KEY` do ambiente:
      `pytest -q` continua rodando em menos de 3s (D-12).
  </acceptance_criteria>
  <reversibility rating="reversible">Constantes de módulo e reestruturação de `try` dentro de uma única função; reverter é `git revert` de um arquivo, sem efeito em dado gravado.</reversibility>
  <done>
    Uma falha de gravação degrada apenas a URL afetada e preserva o briefing já gerado; qualquer
    exceção inesperada no corpo por URL vira item de falha daquela URL em vez de escapar; o lote
    inteiro sobrevive. Reprodução persistida como teste offline.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Linha de cache incompativel com o schema vira miss, nao item de falha</name>
  <files>app/main.py, test_smoke.py</files>
  <read_first>
    - `app/main.py` — **arquivo sendo modificado**. Leia o estado **depois da Task 1**: o bloco de
      cache agora vive dentro do `try` externo. Os pontos de interesse são a chamada `db.buscar`,
      o desempacotamento `dados, nome_extrator, coletado_em = cache`, a construção de
      `BriefingResponse` com `Briefing(**dados)` e o `continue`.
    - `app/schemas.py` — `Briefing` (linhas 17-47): note que `empresa` e `resumo` são obrigatórios
      e sem default; é a ausência de `empresa` que a verificação usou para reproduzir a falha.
    - `app/db.py` — `buscar()` (linhas 41-63): o formato exato da tupla devolvida
      `(dict, str, datetime)` e o ponto onde `json.loads` reconstrói o dicionário.
    - `test_smoke.py` — **arquivo sendo modificado**. O teste escrito na Task 1 é o molde mais
      próximo (mesmo padrão de monkeypatch e `TestClient`).
    - `.planning/phases/05-llm/05-VERIFICATION.md` — bloco `gaps: missing:`, que especifica
      nominalmente "miss de cache" como forma de degradação exigida para esta rota.
    - `.planning/codebase/CONVENTIONS.md` seção "Import Organization" — para posicionar o import
      novo de `pydantic` corretamente.
  </read_first>
  <behavior>
    - `db.buscar` devolvendo uma linha cujo dicionário de briefing não bate com o schema atual
      (falta o campo obrigatório `empresa`): a requisição responde 200 e a URL é **recoletada**,
      voltando com `origem: novo` e `extrator: heuristico` — não com `extrator: falha`, e não com
      um 500.
    - O briefing devolvido é o da extração fresca (`empresa: Acme Tecnologia`), provando que o
      fluxo caiu de fato no caminho de coleta em vez de apenas engolir o erro.
    - Uma linha de cache **válida** continua sendo servida do cache, com `origem: cache` — a
      guarda nova não pode transformar todo acerto de cache em miss.
  </behavior>
  <action>
Mesmo formato da Task 1: teste vermelho primeiro, correção depois.

**Metade 1 — teste de regressão em `test_smoke.py`.**

Acrescente `test_cache_incompativel_com_o_schema_vira_miss(monkeypatch)`. Estrutura:

- `monkeypatch.delenv("LLM_API_KEY", raising=False)` — obrigatório, mesmo motivo da Task 1
  (offline, sem chave, D-12).
- `db.buscar` monkeypatchado para devolver uma tupla de 3 elementos simulando uma linha antiga:
  o dicionário `{"resumo": "texto antigo gravado por um schema anterior"}` — **sem** a chave
  `empresa`, que é obrigatória no `Briefing` atual —, a string `llm` como extrator, e
  `datetime.now(timezone.utc)` como data. Use `llm` de propósito: com a chave ausente,
  `llm_disponivel` é `False`, então a regra de upgrade de D-09 **não** transforma esta linha em
  miss por conta própria — o miss precisa vir da guarda nova, e é isso que o teste isola.
- `main.buscar_html` devolve a constante `HTML`; `db.salvar` devolve `datetime.now(timezone.utc)`.
- Corpo da requisição com **uma** URL e `forcar_atualizacao` ausente ou `false` — o caminho de
  cache só é exercitado assim.
- `TestClient(main.app)` sem `with`, mesmo motivo da Task 1.

Asserções: status 200; `len(dados) == 1`; `origem == "novo"` (é a asserção que prova o
fall-through para recoleta, e não um simples engolir de erro); `extrator == "heuristico"`;
`briefing["empresa"] == "Acme Tecnologia"` (veio da extração fresca sobre `HTML`, não da linha
podre).

Rode `pytest -q` e confirme que o teste novo **falha** antes da metade 2. Depois da Task 1 ele
falha de forma diferente da que a verificação registrou: já não é mais um 500, é
`extrator == "falha"` — o braço catch-all pegou. Confirme que é essa a falha observada; ela é a
evidência de que as duas camadas são distintas.

**Metade 2 — correção em `app/main.py`.**

Adicione `from pydantic import ValidationError` ao bloco de imports de terceiros (o mesmo bloco
de `fastapi`), respeitando a ordenação descrita em `.planning/codebase/CONVENTIONS.md`.

Dentro do ramo `if cache is not None:`, envolva **toda** a montagem da resposta de cache — o
desempacotamento da tupla **e** a construção de `BriefingResponse`, incluindo o
`Briefing(**dados)` — num `try` aninhado que captura `(ValidationError, TypeError)`. Use uma
variável local (por exemplo `resposta_cache`) inicializada em `None`: o `try` a preenche com o
`BriefingResponse` montado, o `except` a deixa em `None`. Só faça `resultados.append(...)` e
`continue` **quando ela não for `None`**; caso contrário, deixe o fluxo seguir naturalmente para
o caminho de coleta logo abaixo, que é o que caracteriza o miss.

Envolver a construção inteira do `BriefingResponse` — e não apenas o `Briefing(**dados)` — é
deliberado: o `extrator` que vem da linha de cache também é validado ali, e o plano 05-09 vai
fechar essa enumeração num `Literal`. Com a guarda no lugar certo, aquela mudança não abre uma
rota de 500 nova.

Deixe um comentário inline explicando o porquê: linha gravada por schema anterior é **dado velho,
não erro do usuário** — o tratamento correto é recoletar, e nunca corrigir ou apagar a linha.
D-10 proíbe qualquer escrita corretiva ou migração de schema: não acrescente instrução alguma de
DDL nem de escrita no banco neste caminho. `app/main.py` não deve conter nenhuma instrução SQL.

Não capture `Exception` largo aqui: a captura estreita `(ValidationError, TypeError)` é o que
mantém a distinção entre "linha velha, recolete" e "erro inesperado, degrade a URL" — o braço
catch-all da Task 1 continua sendo a rede embaixo.
  </action>
  <verify>
    <automated>pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest -q` sai com código 0 e reporta **9 testes passando**.
    - `pytest -q -k cache_incompativel` sai com código 0 e reporta 1 teste passando.
    - Comportamento: `POST /api/briefings` com `forcar_atualizacao` falso e `db.buscar`
      devolvendo um dicionário sem `empresa` responde **200**, com `origem == "novo"`,
      `extrator == "heuristico"` e `briefing.empresa == "Acme Tecnologia"`.
    - Comportamento de não-regressão: `db.buscar` devolvendo uma linha **válida** continua
      produzindo `origem == "cache"`. Verifique manualmente com um `TestClient` ou confirme que
      nenhum teste existente que dependia do caminho de cache quebrou.
    - `python -c "import ast; t=ast.parse(open('app/main.py',encoding='utf-8').read()); ok=any(isinstance(n,ast.ImportFrom) and n.module=='pydantic' and any(a.name=='ValidationError' for a in n.names) for n in ast.walk(t)); assert ok, 'import de ValidationError ausente'; print('ok')"` imprime `ok`.
    - `python -c "import ast; t=ast.parse(open('app/main.py',encoding='utf-8').read()); fn=[n for n in ast.walk(t) if isinstance(n,ast.FunctionDef) and n.name=='gerar_briefings'][0]; hs=[h for n in ast.walk(fn) if isinstance(n,ast.Try) for h in n.handlers]; alvo=[h for h in hs if isinstance(h.type,ast.Tuple)]; assert any({e.id for e in h.type.elts if isinstance(e,ast.Name)}=={'ValidationError','TypeError'} for h in alvo), 'guarda de cache ausente'; print('ok')"` imprime `ok`.
    - `python -c "src=''.join(l for l in open('app/main.py',encoding='utf-8') if not l.lstrip().startswith('#')).upper(); assert not any(k in src for k in ('ALTER TABLE','DROP TABLE','DELETE FROM','UPDATE ')); print('ok')"` imprime `ok` (D-10).
      O filtro de linhas de comentário é proposital: sem ele, um comentário explicando a ausência
      de DDL invalidaria o próprio critério.
    - `git diff --stat -- requirements.txt app/db.py app/schemas.py` não imprime nada.
  </acceptance_criteria>
  <reversibility rating="reversible">`try` aninhado e um import; reverter não deixa resíduo em disco nem no banco — nenhuma linha de `briefings.db` é reescrita ou apagada.</reversibility>
  <done>
    Uma linha de `briefings.db` incompatível com o schema atual é tratada como miss e força
    recoleta, em vez de propagar `ValidationError` ou virar item de falha. Acerto de cache válido
    continua servindo do cache. Reprodução persistida como teste offline.
  </done>
</task>

<task type="auto">
  <name>Task 3: Degrau de falha dupla para de interpolar excecao nao autorada (WR-03)</name>
  <files>app/main.py, test_smoke.py</files>
  <read_first>
    - `app/main.py` — **arquivo sendo modificado**. `_extrair_com_fallback` (linhas 46-84), em
      especial o contraste entre o degrau 2 (linhas 66-77), que aplica corretamente o guarda
      `isinstance(erro_extrator, LLMError)` antes de interpolar, e o degrau 3 (linhas 78-84), que
      interpola sem guarda nenhum. Leia também `MSG_FALHA_GENERICA`, criada na Task 1.
    - `.planning/phases/05-llm/05-REVIEW.md` — `WR-03`, que traz o diagnóstico e a correção
      sugerida.
    - `app/extractor.py` — `HeuristicExtractor.extrair` (linhas 70-83), para confirmar por que
      este degrau é hoje inalcançável em produção: a função é determinística sobre `str` e não
      levanta nas entradas atuais.
    - `test_smoke.py` — **arquivo sendo modificado**; os testes escritos nas Tasks 1 e 2 são o
      molde de monkeypatch a replicar.
  </read_first>
  <action>
Duas mudanças pequenas, no mesmo contrato D-06 que as Tasks 1 e 2 preservam.

**(a) Correção em `app/main.py`.** No degrau 3 de `_extrair_com_fallback` (o `except` interno,
que hoje vincula a exceção do heurístico a um nome e a interpola numa f-string dentro do campo
`resumo`), troque por um `except Exception` **sem vincular nome** e use `MSG_FALHA_GENERICA` —
a constante já criada na Task 1 — como valor de `resumo`. O restante da construção não muda:
`empresa=url`, `confianca="baixa"`, e o retorno continua sendo a 3-tupla
`(briefing, "falha", None)`.

O motivo é o mesmo que o degrau 2 já respeita e este não: `str()` de uma exceção só vai para
campo lido pelo vendedor quando a mensagem é **autorada por nós** (é por isso que o degrau 2
checa `isinstance(..., LLMError)`). O heurístico pode, numa mudança futura, levantar qualquer
coisa. Ajuste o comentário inline vizinho para que ele descreva a regra nos três pontos do
arquivo — degrau 2, degrau 3 e a guarda de gravação da Task 1.

**(b) Teste em `test_smoke.py`.** Acrescente
`test_falha_dupla_devolve_briefing_de_falha_sem_vazar_excecao(monkeypatch)`. É a primeira
cobertura que o degrau 3 recebe.

Para alcançá-lo, os dois extratores precisam falhar. Defina no corpo do teste uma classe cujo
`extrair` levanta `RuntimeError`, com atributo `nome` igual a `llm`, e monkeypatche **duas**
coisas em `main`: `escolher_extrator`, para devolver uma instância dela, e `HeuristicExtractor`,
para que o degrau 2 também levante — note que `_extrair_com_fallback` instancia
`HeuristicExtractor()` a partir do namespace de `main`, então patchear ali é suficiente e não
altera `app/extractor.py`.

Complete o cenário como nas tasks anteriores: `delenv` de `LLM_API_KEY`, `main.buscar_html`
devolvendo `HTML`, `db.buscar` devolvendo `None`, `db.salvar` devolvendo
`datetime.now(timezone.utc)`, `forcar_atualizacao: true`, uma URL, `TestClient` sem `with`.

Asserções: status 200; `extrator == "falha"`; `briefing["confianca"] == "baixa"`;
`briefing["resumo"] == main.MSG_FALHA_GENERICA` por **igualdade exata** — de novo, a igualdade
exata é o que prova que o texto da exceção não vazou, sem precisar de asserção negativa;
`degradado is None`.

Confirme também que `db.salvar` **não** foi chamado para este item (falha não vai para o cache):
registre as chamadas numa lista dentro do duplo de `db.salvar` e afirme que ela está vazia.
  </action>
  <verify>
    <automated>pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest -q` sai com código 0 e reporta **10 testes passando** em menos de 3s.
    - `pytest -q -k falha_dupla` sai com código 0 e reporta 1 teste passando.
    - Comportamento: com ambos os extratores levantando, `POST /api/briefings` devolve **200**,
      `extrator == "falha"`, `briefing.confianca == "baixa"`, `briefing.resumo` **exatamente
      igual** a `main.MSG_FALHA_GENERICA` e `degradado` nulo.
    - Comportamento: `db.salvar` não recebe chamada alguma nesse cenário.
    - `python -c "import ast; t=ast.parse(open('app/main.py',encoding='utf-8').read()); fn=[n for n in ast.walk(t) if isinstance(n,ast.FunctionDef) and n.name=='_extrair_com_fallback'][0]; hs=[h for n in ast.walk(fn) if isinstance(n,ast.Try) for h in n.handlers]; assert any(h.name is None for h in hs), 'degrau 3 ainda vincula a excecao a um nome'; print('ok')"` imprime `ok`.
    - `python -c "import app.main as m, inspect; s=inspect.getsource(m._extrair_com_fallback); assert 'MSG_FALHA_GENERICA' in s; print('ok')"` imprime `ok`.
    - `git diff --stat -- requirements.txt app/extractor.py app/db.py app/schemas.py` não imprime
      nada.
  </acceptance_criteria>
  <reversibility rating="reversible">Troca de uma f-string por uma constante já existente, mais um teste aditivo; um arquivo, sem efeito em contrato de API nem em dado gravado.</reversibility>
  <done>
    Os três pontos de `app/main.py` que escrevem mensagem lida pelo vendedor seguem a mesma regra
    (só interpolamos exceção autorada por nós), e o degrau de falha dupla ganha sua primeira
    cobertura de teste.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| navegador -> API (`POST /api/briefings`) | lista de URLs controlada pelo usuário cruza aqui; um lote de 10 URLs é uma unidade de falha compartilhada |
| site de terceiro -> processo da API | HTML não confiável entra via `buscar_html` e chega ao extrator |
| `briefings.db` -> processo da API | linha gravada por versão anterior do schema é **dado não confiável na leitura**, mesmo tendo sido escrita por nós |
| processo -> SQLite (escrita) | `db.salvar` serializa escritas; sob concorrência, falha é resultado esperado, não excepcional |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-50 | Denial of Service | `gerar_briefings`, corpo por URL | high | mitigate | Task 1: `try` único cobrindo busca de cache, fetch, extração e gravação, com braço `except Exception` sem nome. Uma URL ruim deixa de poder cancelar as outras 9 do lote (L-02) |
| T-05-51 | Information Disclosure | `BriefingResponse.degradado` e `Briefing.resumo` | medium | mitigate | Tasks 1 e 3: toda mensagem que chega ao vendedor é literal autorado (`AVISO_CACHE_INDISPONIVEL`, `MSG_FALHA_GENERICA`); nenhuma exceção não autorada é interpolada; truncamento em 200 caracteres preservado (D-06). Travado por asserção de igualdade exata nos testes |
| T-05-52 | Tampering | linha de `briefings.db` escrita por schema anterior | medium | mitigate | Task 2: linha incompatível vira miss e força recoleta. Nenhuma DDL, nenhum `DELETE`/`UPDATE` corretivo — a linha podre é ignorada, não "consertada" (D-10) |
| T-05-53 | Denial of Service | escrita concorrente em SQLite (`database is locked`) | medium | accept | A causa raiz — SQLite serializa escritores e a rota é síncrona — **não** é corrigida aqui: pool de conexões e Postgres estão fora de escopo por decisão do usuário (`05-CONTEXT.md`, Deferred Ideas). O que este plano garante é que a falha degrada por URL em vez de por lote. ASVS L1 não exige disponibilidade sob concorrência |
| T-05-54 | Repudiation | ausência de log estruturado da degradação | low | accept | O projeto não tem logging estruturado nesta fase (`.planning/codebase/CONVENTIONS.md`); o sinal de degradação viaja na própria resposta ao vendedor (D-05/D-07), que é o consumidor real. Suficiente para ASVS L1 |
| T-05-55 | Elevation of Privilege | rotas sem autenticação | medium | accept | Autenticação é escopo declarado da Fase 6 (SPEC §15); ASVS L1 não exige controle de acesso em MVP sem dados de usuário. Inalterado por este plano |

**Nível ASVS:** 1. **Bloqueio configurado:** `high`. A única ameaça `high` deste plano (T-05-50) é
`mitigate` e tem verificação automatizada na Task 1 — nenhum item `high` fica pendente.

**Package legitimacy gate:** não se aplica. Nenhum pacote é instalado neste plano (L-06, D-01);
`requirements.txt` permanece intocado, e há critério de aceite explícito para isso em cada task.
</threat_model>

<verification>
- `pytest -q` verde com **10 testes**, offline, sem chave de API e sem rede, em menos de 3s (D-12).
- `POST /api/briefings` com 2 URLs e falha de gravação na primeira: **200**, os 2 itens presentes,
  o segundo intacto, o primeiro com o briefing preservado e o aviso de cache no campo `degradado`.
- `POST /api/briefings` com linha de cache incompatível: **200**, `origem: novo`, briefing vindo
  da recoleta.
- `POST /api/briefings` com os dois extratores falhando: **200**, `extrator: falha`, `resumo`
  igual ao literal autorado.
- `git diff -- requirements.txt app/db.py app/schemas.py app/extractor.py` vazio.
- Nenhum `ALTER`/`DROP`/`DELETE` em `app/main.py` (D-10).
</verification>

<success_criteria>
- **L-02 vira invariante estrutural:** todo o corpo por URL de `gerar_briefings` está dentro de um
  `try` com braços `except FetchError` e `except Exception`; nenhuma exceção do corpo por URL
  escapa para o chamador.
- Falha de gravação degrada só a URL afetada e **preserva o briefing já gerado**, sinalizando a
  degradação em `BriefingResponse.degradado` (D-05, D-06).
- Linha de cache incompatível com o schema atual vira **miss** e força recoleta (D-10: sem DDL,
  sem escrita corretiva).
- Os três pontos de `app/main.py` que escrevem mensagem lida pelo vendedor seguem a mesma regra de
  interpolação (D-06).
- As duas reproduções da `05-VERIFICATION.md` existem como testes offline persistidos e a suíte
  segue sem chave e sem rede.
</success_criteria>

## Auditoria de fontes — batelada de fechamento de lacunas (planos 05-08 e 05-09)

Auditoria das quatro fontes antes de fechar a batelada. A auditoria completa da fase original está
em `05-01-PLAN.md` e **não é reaberta aqui** — 21 dos 23 must-haves já foram verificados contra o
código em disco.

### GOAL — `05-VERIFICATION.md`, bloco `gaps:`

| Item | Status | Onde |
|---|---|---|
| `L-02` — envolver `db.salvar(...)` para degradar a URL individual | COVERED | 05-08 T1 |
| `L-02` — envolver o `Briefing(**dados)` do caminho de cache, tratando a linha como miss | COVERED | 05-08 T2 |
| `L-02` — não propagar a exceção para fora do laço | COVERED | 05-08 T1 (braço catch-all, cobre a classe e não só as duas rotas nomeadas) |
| Reproduções persistidas como teste offline | COVERED | 05-08 T1, T2 |

### REQ — IDs de requisito

`phase_req_ids` é **null**. Este projeto não tem `REQUIREMENTS.md` nem `ROADMAP.md`, por decisão de
processo registrada no topo de `05-CONTEXT.md`; os IDs de decisão travada fazem esse papel e estão
no campo `requirements` de cada plano. **`L-02` aparece em ambos os planos da batelada.**
*Spec-less probe fallback skipped: phase has no requirement IDs to probe (visible skip).*

### RESEARCH — nenhum

Não existe `RESEARCH.md`. O modo `--gaps` pula a pesquisa por desenho, e `05-VALIDATION.md`
depende dela — o usuário foi consultado e optou explicitamente por seguir sem os dois. Nenhum item
de pesquisa a auditar; ausência registrada, não suprida com estratégia inventada.

### CONTEXT — `05-CONTEXT.md` e `05-REVIEW.md`

| Item | Status | Onde |
|---|---|---|
| `L-02` (travada, "NÃO reabrir") | COVERED | 05-08 T1, T2 |
| `D-05`/`D-06` — visibilidade da degradação preservada, mensagem curta, sem stack trace, 200 chars | COVERED | 05-08 T1, T3 |
| `D-10` — sem migração de schema, sem DDL destrutiva | COVERED | 05-08 T2 (critério de aceite explícito) |
| `D-12` — testes offline, sem chave e sem rede | COVERED | 05-08 T1, T2, T3; 05-09 T1, T2, T3 |
| `L-06`/`D-01` — sem dependência nova | COVERED | critério de aceite em todas as tasks das duas |
| `WR-01` — `extrator` como `Literal` fechado (SPEC §8) | COVERED | 05-09 T1 |
| `WR-02` — testes ausentes para D-09, D-02 e D-11 | COVERED | 05-09 T2, T3 |
| `WR-03` — interpolação sem guarda no braço de falha dupla | COVERED | 05-08 T3 |

**Nenhum item MISSING.** Nada foi reduzido, adiado ou simplificado em relação às fontes. Os três
achados de warning do `05-REVIEW.md` foram **todos planejados**, nenhum descartado em silêncio.

**Exclusões legítimas (não são lacunas):**
- Os itens de `## Deferred Ideas` e de "Escopo cortado explicitamente" do `05-CONTEXT.md` —
  `conteudo_hash`, `config.py` com as 8 variáveis, `tests/` em 4 arquivos, Docker/README,
  autenticação (Fase 6), conexão SQLite não fechada, `@app.on_event` depreciado.
- `IN-01` e `IN-02` do `05-REVIEW.md`: `IN-02` (`on_event` depreciado) está **explicitamente fora
  de escopo por decisão do usuário** no `05-CONTEXT.md`, e o próprio review nota que mexer nele
  quebraria o truque do `TestClient` sem `with` de que os testes desta batelada dependem. `IN-01`
  (reexecução redundante do heurístico quando ele já é o extrator primário) é otimização sem
  mudança de comportamento; fica registrado como follow-up, não como lacuna.
- Os dois limites residuais confirmados pela verificação como corretamente caracterizados: o galho
  `_SemJsonSchema` nunca exercitado contra o Groq real, e a latência por URL com estimativa de
  ordem de grandeza. O plano 05-09 T3 **não** fecha o primeiro — ele trava a lógica do galho com
  duplo, o que é coisa diferente de exercitá-lo contra o provedor. O limite continua registrado
  em `COVERAGE.md` e no `05-05-SUMMARY.md`.

## Artifacts this phase produces

**Produzidos por este plano (05-08):**
- `app/main.py::AVISO_CACHE_INDISPONIVEL` — constante de módulo, literal autorado do aviso de
  falha de gravação
- `app/main.py::MSG_FALHA_GENERICA` — constante de módulo, literal autorado do briefing de falha;
  usada pelo braço catch-all (T1) e pelo degrau 3 de `_extrair_com_fallback` (T3)
- `app/main.py::gerar_briefings` — reestruturada: corpo por URL inteiro dentro de um `try`, braço
  `except Exception`, `try` aninhado em torno de `db.salvar`, `try` aninhado em torno da montagem
  da resposta de cache, import novo de `pydantic.ValidationError`
- `app/main.py::_extrair_com_fallback` — degrau 3 sem interpolação de exceção não autorada
- `test_smoke.py::test_falha_ao_salvar_no_cache_nao_derruba_o_lote`
- `test_smoke.py::test_cache_incompativel_com_o_schema_vira_miss`
- `test_smoke.py::test_falha_dupla_devolve_briefing_de_falha_sem_vazar_excecao`

**Produzidos pelo plano 05-09 (roll-up da batelada):**
- `app/schemas.py::BriefingResponse.extrator` — tipo fechado em `Literal["llm","heuristico","falha"]`
- `test_smoke.py::test_cache_com_extrator_fora_da_enumeracao_vira_miss`
- `test_smoke.py::test_cache_heuristico_vira_miss_quando_llm_disponivel`
- `test_smoke.py::test_cache_llm_sobrevive_quando_llm_indisponivel`
- `test_smoke.py::test_montar_mensagens_remove_delimitador_forjado`
- `test_smoke.py::test_degradacao_json_schema_faz_exatamente_uma_segunda_chamada`
- `test_smoke.py::test_segundo_400_de_json_schema_vira_llmerror`

**Nenhum arquivo novo é criado por esta batelada.** `requirements.txt`, `app/db.py`,
`app/extractor.py`, `app/config.py`, `static/index.html` e `.env.example` permanecem intocados.

<output>
Create `.planning/phases/05-llm/05-08-SUMMARY.md` when done
</output>
