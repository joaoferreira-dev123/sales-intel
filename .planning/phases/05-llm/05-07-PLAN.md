---
phase: 05-llm
plan: 07
type: execute
wave: 6
depends_on: ["05-04", "05-06"]
files_modified:
  - app/db.py
  - app/main.py
autonomous: true
requirements: [D-09, D-10]

estimate:
  tokens: 28000
  raw_tokens: 14000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "com o LLM disponivel, uma entrada de cache gravada por heuristico e tratada como miss e forca recoleta (D-09)"
    - "com o LLM disponivel, uma entrada gravada por llm continua valendo os 7 dias normais"
    - "sem o LLM disponivel, o comportamento do cache e identico ao de antes desta fase"
    - "nenhuma coluna e criada, alterada ou removida, e briefings.db nao e apagado (D-10)"
  artifacts:
    - "app/db.py::buscar(url, llm_disponivel=False)"
  key_links:
    - "main.py passa llm_disponivel=bool(config.llm_api_key()) a db.buscar, a mesma fonte de escolher_extrator() e de /health"
    - "a regra vive na leitura do cache, nao no schema — CREATE TABLE permanece intocado"
---

<objective>
Sem esta regra, URLs ja coletadas voltam do cache marcadas `extrator: heuristico` mesmo com o
LLM ligado — visivel na tela durante a apresentacao, e exatamente a impressao errada: pareceria
que o LLM nao esta funcionando.

D-09 resolve isso na **leitura** do cache: entrada gravada por `heuristico` vira *miss* quando o
LLM esta disponivel, forcando recoleta com o extrator melhor. Entrada gravada por `llm` segue a
validade normal de 7 dias.

Purpose: item 6 da tabela `<execution_order>` do CONTEXT.md — o ultimo da fila, so rende com o
LLM ligado.
Output: `db.buscar()` com a regra de upgrade e `main.py` informando a disponibilidade.
</objective>

<planner_notes>
**D-10 e restricao dura deste plano.** `briefings.db` ja existe com dados e
`CREATE TABLE IF NOT EXISTS` nao adiciona coluna a tabela existente. Migracao de schema horas
antes da demonstracao e risco sem retorno. Por isso: **nenhum `ALTER TABLE`, nenhum `DROP`,
nenhum `DELETE`, e o arquivo do banco nao e apagado nem recriado.** D-09 foi desenhado
justamente para nao exigir schema novo — a coluna `extrator` que a regra consulta ja existe desde
a Fase 3.

`conteudo_hash` (SPEC §8) continua adiado. Ele resolve outro problema — *a pagina mudou* — e nao
este — *o extrator melhorou*. Esta registrado nas ideias adiadas do CONTEXT.md.

**Sem teste novo, deliberadamente.** D-12 fixa **quatro** testes em `test_smoke.py` e os quatro ja
existem (planos 03 e 05). A regra de D-09 e verificada por comando automatizado sobre um banco
temporario no `<verify>` da Task 1, sem acrescentar um quinto teste ao arquivo e sem tocar no
banco real.
</planner_notes>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@SPEC-sales-intel.md
@.planning/phases/05-llm/05-CONTEXT.md
@.planning/phases/05-llm/05-PATTERNS.md
@app/db.py
@app/main.py
@app/config.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Regra de upgrade de extrator na leitura do cache (D-09, D-10)</name>
  <files>app/db.py</files>
  <read_first>
    - `app/db.py` — arquivo inteiro. Alvos: `buscar()` (linhas 41-56, incluindo o comentario
      inline `# existe, mas venceu`, que e o modelo de estilo a replicar) e `criar_tabelas()`
      (linhas 27-38), que **nao** pode ser tocado.
    - `.planning/phases/05-llm/05-PATTERNS.md` secao "`app/db.py` — regra de upgrade de extrator
      em `buscar()`" — traz a funcao atual verbatim e o formato do comentario a acrescentar.
    - `.planning/phases/05-llm/05-CONTEXT.md` D-09 e D-10.
    - `SPEC-sales-intel.md` §8 — a coluna `extrator` (`llm` / `heuristico` / `falha`) e a validade
      de 7 dias.
  </read_first>
  <action>
Em `app/db.py`, acrescente a regra de upgrade a leitura do cache — **somente na leitura**.

A assinatura de `buscar` passa a
`def buscar(url: str, llm_disponivel: bool = False) -> tuple[dict, str, datetime] | None:`.
O default `False` e obrigatorio: mantem todos os chamadores e testes existentes validos sem
edicao, e mantem o comportamento anterior quando nao ha LLM.

Estenda a docstring para dizer, em uma frase, que uma entrada gravada pelo heuristico e tratada
como ausente quando o LLM esta disponivel.

Logo apos a verificacao de validade que ja existe (`if datetime.now(timezone.utc) - coletado_em >
VALIDADE: return None  # existe, mas venceu`), acrescente uma segunda condicao de *miss*:
quando `llm_disponivel` for verdadeiro **e** `row["extrator"] == "heuristico"`, devolva `None`.
Use um comentario inline no mesmo estilo do que ja esta la, explicando o *porque*:
`# heuristico com LLM ligado: forca recoleta para subir a qualidade`.

O `return` final permanece exatamente como esta.

**Proibicoes duras (D-10):**
- Nao toque em `criar_tabelas()`. Nenhuma coluna nova, nenhuma alteracao de schema.
- Nao escreva `ALTER TABLE`, `DROP TABLE`, `DELETE FROM` nem qualquer `UPDATE` neste plano.
- Nao apague, mova nem recrie `briefings.db`, e nao mude `DB_PATH`.
- Nao implemente `conteudo_hash` — adiado por D-10.
- Nao altere `salvar()` nem `listar()`.
  </action>
  <verify>
    <automated>pytest -q && python -c "
import tempfile
from pathlib import Path
import app.db as db
db.DB_PATH = Path(tempfile.mkdtemp()) / 'teste.db'
db.criar_tabelas()
db.salvar('https://a.com.br', {'empresa': 'A', 'resumo': 'r'}, 'heuristico')
assert db.buscar('https://a.com.br') is not None, 'default deveria manter o comportamento antigo'
assert db.buscar('https://a.com.br', llm_disponivel=False) is not None
assert db.buscar('https://a.com.br', llm_disponivel=True) is None, 'heuristico deveria virar miss'
db.salvar('https://b.com.br', {'empresa': 'B', 'resumo': 'r'}, 'llm')
assert db.buscar('https://b.com.br', llm_disponivel=True) is not None, 'llm deveria seguir a validade normal'
assert db.buscar('https://b.com.br', llm_disponivel=False) is not None
print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `db.buscar(url)` sem o novo argumento se comporta exatamente como antes (default `False`).
    - Entrada gravada com `extrator='heuristico'` devolve `None` quando `llm_disponivel=True` e
      devolve a tupla quando `llm_disponivel=False`.
    - Entrada gravada com `extrator='llm'` devolve a tupla nos dois casos (validade normal de
      7 dias preservada).
    - `python -c "import inspect, app.db as db; p=inspect.signature(db.buscar).parameters; assert p['llm_disponivel'].default is False; print('ok')"` imprime `ok`.
    - `grep -v '^\s*#' app/db.py | grep -ciE 'alter table|drop table|delete from|os\.remove|unlink' || true` imprime `0` (D-10).
    - `git diff app/db.py` nao mostra nenhuma alteracao dentro de `criar_tabelas()`.
    - `pytest -q` sai com codigo 0 e reporta 7 testes.
    - `briefings.db` nao foi apagado nem recriado: o arquivo mantem o mesmo tamanho e a mesma
      data de modificacao de antes da task (se ele existir na maquina).
  </acceptance_criteria>
  <done>
    O cache sabe distinguir "fresco" de "bom o bastante": um briefing heuristico deixa de ser
    devolvido quando existe um extrator melhor disponivel — sem tocar no schema.
  </done>
</task>

<task type="auto">
  <name>Task 2: main.py informa a disponibilidade do LLM ao consultar o cache</name>
  <files>app/main.py</files>
  <read_first>
    - `app/main.py` — estado apos os planos 01 e 06. Alvos: a chamada `cache = db.buscar(url)`
      dentro do laco de `gerar_briefings`, e o import `from . import config` ja acrescentado pelo
      plano 06 (Task 3).
    - `app/config.py` — `llm_api_key()`, a fonte unica.
    - `app/extractor.py` — `escolher_extrator()`, para confirmar que a decisao de cache e a
      decisao de extrator consultam a mesma coisa.
    - `test_smoke.py` — `test_extrator_que_falha_nao_derruba_a_requisicao` monkeypatcha
      `db.buscar` com assinatura tolerante (`*args, **kwargs`), justamente para sobreviver a esta
      mudanca. Confirme que continua assim.
  </read_first>
  <action>
Em `app/main.py`, dentro do laco de `gerar_briefings`, troque a consulta de cache
`cache = db.buscar(url)` por `cache = db.buscar(url, llm_disponivel=bool(config.llm_api_key()))`.

A avaliacao acontece **dentro do laco**, junto da consulta, pelo mesmo motivo que a escolha de
extrator e por URL (L-02): a disponibilidade e reavaliada a cada URL e nao congela no inicio do
lote. Deixe um comentario inline de uma linha explicando que a fonte e a mesma consultada por
`escolher_extrator()` e por `/health`, para que os tres nunca discordem.

Se `from . import config` ainda nao estiver no bloco de imports locais (por exemplo, se o plano 06
tiver sido cortado por tempo), acrescente-o.

Nao mexa em mais nada: `forcar_atualizacao` continua tendo precedencia, o caminho de cache
continua construindo `BriefingResponse` com `origem="cache"` e sem o parametro `degradado`, e
`db.salvar` continua gravando `nome_extrator`.
  </action>
  <verify>
    <automated>pytest -q && python -c "
import os, json, tempfile
from pathlib import Path
from datetime import datetime, timezone
from fastapi.testclient import TestClient
import app.db as db, app.main as m
db.DB_PATH = Path(tempfile.mkdtemp()) / 'teste.db'
db.criar_tabelas()
db.salvar('https://acme.com.br/', {'empresa': 'Acme', 'resumo': 'texto salvo antes'}, 'heuristico')
c = TestClient(m.app)
os.environ.pop('LLM_API_KEY', None)
r = c.post('/api/briefings', json={'urls': ['https://acme.com.br/'], 'forcar_atualizacao': False})
assert r.status_code == 200 and r.json()[0]['origem'] == 'cache', r.json()
os.environ['LLM_API_KEY'] = 'chave-de-teste-sem-valor'
import inspect
assert 'llm_disponivel' in inspect.getsource(m.gerar_briefings)
print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'db.buscar(url, llm_disponivel=' app/main.py` imprime `1`.
    - `grep -v '^\s*#' app/main.py | grep -c 'db.buscar(url)' || true` imprime `0`.
    - Sem `LLM_API_KEY` no ambiente, uma URL com entrada `heuristico` no cache volta com
      `origem == "cache"` (comportamento inalterado).
    - Com `LLM_API_KEY` no ambiente, a mesma URL deixa de vir do cache: o caminho de recoleta e
      acionado (`origem == "novo"`), provando a regra de upgrade ponta a ponta.
    - A disponibilidade e lida **dentro** do laco, junto da consulta de cache — nao ha variavel
      calculada antes do `for`.
    - `pytest -q` sai com codigo 0 e reporta 7 testes; em especial
      `test_extrator_que_falha_nao_derruba_a_requisicao` continua verde sem edicao, graças a
      assinatura tolerante do monkeypatch escrita no plano 03.
  </acceptance_criteria>
  <done>
    Com o LLM ligado, nenhuma URL volta a tela marcada `extrator: heuristico` so porque foi
    coletada antes da chave chegar.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| aplicacao -> `briefings.db` | banco ja populado, com dados de coletas anteriores |
| ambiente -> decisao de cache | `LLM_API_KEY` passa a influenciar o que o cache devolve |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-36 | Tampering | perda de dados por migracao de schema as vesperas da demo | high | mitigate | D-10: a regra vive na leitura, nao no schema. Criterio de aceite faz grep negativo por `ALTER TABLE`, `DROP TABLE`, `DELETE FROM`, `os.remove` e `unlink`, e exige `criar_tabelas()` intocado |
| T-05-37 | Denial of Service | recoleta em massa ao ligar a chave | medium | accept | o limite de 1 a 10 URLs por requisicao (RF1, `BriefingRequest`) ja limita o lote; a recoleta e o comportamento desejado de D-09 e acontece uma unica vez por URL |
| T-05-38 | Information Disclosure | `LLM_API_KEY` vazando pela decisao de cache | high | mitigate | Task 2 usa apenas `bool(config.llm_api_key())`; o valor nunca e passado adiante, comparado por igualdade nem gravado |
| T-05-39 | Spoofing | `/health`, `escolher_extrator()` e o cache discordando sobre o modo de operacao | low | mitigate | os tres consultam `config.llm_api_key()`, fonte unica estabelecida no plano 04 |
| T-05-40 | Tampering | dado servido do cache sem revalidacao de conteudo | low | accept | `conteudo_hash` (SPEC §8) resolve *pagina mudou*, nao *extrator melhorou*; adiado por D-10 e registrado nas ideias adiadas |

Nenhum pacote novo e instalado neste plano (L-06); o package-legitimacy gate nao se aplica.
</threat_model>

<verification>
- `pytest -q` verde com 7 testes.
- Regra de upgrade provada sobre banco temporario: `heuristico` + LLM ligado vira miss; `llm`
  segue a validade de 7 dias.
- Nenhuma instrucao de DDL destrutiva no diff.
</verification>

<success_criteria>
- D-09: entrada `heuristico` vira miss com o LLM disponivel; entrada `llm` mantem 7 dias.
- D-10: zero mudanca de schema, zero perda de dados, `conteudo_hash` segue adiado.
- Fonte unica de disponibilidade compartilhada por cache, extrator e `/health`.
</success_criteria>

## Artifacts this phase produces

**Produzidos por este plano:**
- `app/db.py::buscar(url: str, llm_disponivel: bool = False)` com a regra de upgrade de extrator
- `app/main.py` consultando o cache com `llm_disponivel=bool(config.llm_api_key())`

**Demais artefatos da fase (roll-up completo):**
- Plano 01: `app/main.py::_extrair_com_fallback`, `::BASE_DIR`, `::STATIC_DIR`.
- Plano 02: `app/fetcher.py::ROBOTS_TIMEOUT`; `pode_raspar()` reescrito; `static/index.html` em
  UTF-8 sem BOM.
- Plano 03: `test_smoke.py::test_escolher_extrator_sem_chave_devolve_heuristico`,
  `::test_escolher_extrator_com_chave_devolve_llm`,
  `::test_extrator_que_falha_nao_derruba_a_requisicao`.
- Plano 04: `app/config.py` com `llm_api_key()`, `LLM_MODELO`, `LLM_MAX_CHARS`; `.env.example`.
- Plano 05: `app/extractor.py::LLMError`, `::_SemJsonSchema`, `::LLM_BASE_URL`, `::LLM_TIMEOUT`,
  `::DELIM_INICIO`, `::DELIM_FIM`, `::LLMExtractor._montar_mensagens`,
  `::LLMExtractor._chamar_provedor`, `::LLMExtractor.extrair`;
  `test_smoke.py::test_llm_sem_chave_levanta_erro_claro`.
- Plano 06: `app/schemas.py::BriefingResponse.degradado`; `/health` com `llm_disponivel`;
  `static/index.html` classe `.tag-aviso` e `<span>` condicional em `render()`.

<output>
Create `.planning/phases/05-llm/05-07-SUMMARY.md` when done
</output>
