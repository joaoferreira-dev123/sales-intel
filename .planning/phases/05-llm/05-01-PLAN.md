---
phase: 05-llm
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/main.py
autonomous: true
requirements: [L-01, L-02, L-05]

estimate:
  tokens: 34000
  raw_tokens: 17000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "com LLM_API_KEY exportada e um extrator que levanta excecao, POST /api/briefings responde 200 e nenhuma URL do lote e perdida (L-02)"
    - "sem LLM_API_KEY configurada, POST /api/briefings responde 200 e os itens saem com extrator: heuristico (L-05)"
    - "nenhuma falha de extrator produz HTTP 500: falha de extrator vira briefing de confianca baixa"
    - "com o servidor iniciado a partir de qualquer working directory, GET / devolve 200 e o corpo comeca com <!DOCTYPE html>"
  artifacts:
    - "app/main.py::_extrair_com_fallback"
    - "app/main.py::BASE_DIR"
    - "app/main.py::STATIC_DIR"
  key_links:
    - "_extrair_com_fallback chama escolher_extrator() uma vez por URL, dentro do laco (L-02)"
    - "escolher_extrator() deixa de ser chamado antes do laco em gerar_briefings"
    - "StaticFiles(directory=STATIC_DIR) e FileResponse(STATIC_DIR / 'index.html') usam caminho absoluto"
---

<objective>
Corrigir o defeito que motiva a fase inteira: hoje `escolher_extrator()` devolve
`LLMExtractor` no instante em que `LLM_API_KEY` existir, `LLMExtractor.extrair()` termina em
erro, e `main.py:75` so captura `FetchError` — ou seja, exportar a variavel transforma toda
requisicao em HTTP 500 e derruba junto as URLs do lote que ja tinham dado certo.

Este plano fecha esse buraco (correcao 1) e a quebra de caminho relativo dos arquivos
estaticos (correcao 4). Depois deste plano o sistema fica estavel **mesmo com a chave
exportada** — que e a pre-condicao para todo o resto da fase poder falhar para dentro de um
caminho seguro.

Purpose: item 1 da tabela `<execution_order>` do CONTEXT.md. Se o tempo acabar aqui, a demo
roda estavel no heuristico, sem o crash.
Output: `app/main.py` com selecao de extrator por URL e degradacao em tres degraus.
</objective>

<planner_notes>
**Subordinacao do tracer-first (registrada como decisao, nao como esquecimento).** O padrao
GSD manda liderar a fase com uma fatia end-to-end do `LLMExtractor`. Aqui isso foi
deliberadamente invertido: o CONTEXT.md `<execution_order>` e decisao travada do usuario e
raciocina o oposto — *"a garantia estrutural e que o caminho nao verificado so consegue falhar
para dentro do caminho verificado — LLM quebra, heuristico entrega. E por isso que o item 2 vem
antes do item 4, e nao depois."* Nao existe chave de API, logo a fatia do LLM **nao e
verificavel contra um modelo real nesta fase**; endurecer primeiro a rede de seguranca e o que
torna a fatia nao verificavel segura. O `LLMExtractor` (plano 05) continua sendo a fatia
vertical da fase — apenas sequenciada depois do proprio colchao.

**Probe spec-less: pulado, visivelmente.** Nao existe `05-SPEC.md` nesta fase, logo nao ha
`## Edge Coverage` nem `## Prohibitions` para levantar. *Spec-less probe fallback skipped: phase
has no requirement IDs to probe (visible skip).* Os `must_haves` foram derivados do criterio de
pronto da SPEC §15 e das decisoes travadas L-01..L-06 / D-01..D-12.

**Premissa explicita do planejador:** nao ha `LLM_API_KEY` disponivel. Nenhuma verdade que
dependa de um modelo real e verificavel nesta fase; a unica delas esta marcada como
`verification: backstop` no plano 05.
</planner_notes>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@SPEC-sales-intel.md
@.planning/phases/05-llm/05-CONTEXT.md
@.planning/phases/05-llm/05-PATTERNS.md
@app/main.py
@app/extractor.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Selecao de extrator por URL com degradacao em tres degraus</name>
  <files>app/main.py</files>
  <read_first>
    - `app/main.py` — estado atual completo: `escolher_extrator()` na linha 47 (fora do laco),
      o laco `for url_obj in req.urls` (linhas 50-93) e o bloco `except FetchError` (linhas 75-83)
      que e o analog exato do padrao "falha vira resultado".
    - `app/extractor.py` — contrato `Extractor` (Protocol, linhas 31-36), `HeuristicExtractor`
      (linhas 39-61) e `escolher_extrator()` (linhas 92-100).
    - `.planning/phases/05-llm/05-PATTERNS.md` secao "`app/main.py` — segundo braco de erro" —
      traz o bloco `except FetchError` verbatim como padrao a replicar.
    - `SPEC-sales-intel.md` §6 (resiliencia: LLM indisponivel nao derruba a requisicao) e §10
      (falha por URL nao vira erro HTTP).
  </read_first>
  <action>
Reescreva o caminho de extracao de `gerar_briefings` em `app/main.py` para cumprir L-01, L-02 e
L-05.

**1. Nova funcao auxiliar de modulo, definida acima de `gerar_briefings`:**

`def _extrair_com_fallback(url: str, titulo: str, texto: str) -> tuple[Briefing, str]:`

Docstring em pt-BR no estilo dos demais modulos, explicando o *porque*: nenhuma rota desta
funcao levanta excecao, porque falha vira resultado (SPEC §6/§10) e o vendedor prefere resultado
parcial a erro. A funcao devolve `(briefing, nome_extrator)` e tenta tres degraus, nesta ordem:

- **Degrau 1:** `extrator = escolher_extrator()` — a chamada acontece **dentro** desta funcao,
  portanto uma vez por URL (L-02, L-01: quem chama continua sem saber qual implementacao roda).
  Chame `extrator.extrair(url, titulo, texto)` e devolva `(briefing, extrator.nome)`.
- **Degrau 2:** em `except Exception`, chame `HeuristicExtractor().extrair(url, titulo, texto)` e
  devolva `(briefing, "heuristico")`. O `except` e propositalmente largo — **qualquer** falha do
  extrator principal precisa degradar, e o tipo de erro do LLM ainda nao existe neste ponto da
  fase. Deixe um comentario inline explicando essa escolha.
- **Degrau 3:** se o degrau 2 tambem levantar, devolva um briefing de falha no mesmo formato do
  bloco `except FetchError` ja existente: `Briefing(empresa=url, resumo=f"Nao foi possivel gerar
  o briefing. {e}", confianca="baixa")` e `nome_extrator = "falha"`.

Nesta wave a funcao devolve uma **2-tupla**. O plano 06 a estende para 3-tupla ao introduzir o
campo de degradacao (D-05/D-06) — nao antecipe esse campo aqui.

**2. No corpo de `gerar_briefings`:**

- Remova a linha `extrator = escolher_extrator()` que hoje fica **antes** do laco (linha 47).
  Nenhuma outra referencia a `extrator` deve sobrar no corpo da rota.
- Dentro do `try`, depois de `titulo, texto = extrair_texto(html)`, substitua as tres linhas
  atuais (`briefing = extrator.extrair(...)`, `db.salvar(...)`, `nome_extrator = extrator.nome`)
  por: chamada a `_extrair_com_fallback(url, titulo, texto)` desempacotando em
  `briefing, nome_extrator`; em seguida, **nao gravar falha no cache** — quando
  `nome_extrator == "falha"`, use `coletado_em = datetime.now(timezone.utc)`; caso contrario,
  `coletado_em = db.salvar(url, briefing.model_dump(), nome_extrator)`. Mantenha
  `origem = "novo"`.
- O bloco `except FetchError as e:` existente fica **exatamente como esta**. Ele cobre a falha de
  rede; o novo caminho cobre a falha de extrator. Sao dois bracos distintos.

**3. Imports:** a linha `from .extractor import escolher_extrator` passa a
`from .extractor import HeuristicExtractor, escolher_extrator` (ordem alfabetica, mesmo estilo
da linha `from .fetcher import FetchError, buscar_html, extrair_texto`).

Nao toque em `app/extractor.py`, `app/db.py` nem `app/schemas.py` nesta task.
  </action>
  <verify>
    <automated>pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest -q` sai com codigo 0 e reporta 3 testes passando (os 3 existentes seguem verdes).
    - `grep -c 'escolher_extrator' app/main.py` imprime `2` (o import e a unica chamada, dentro de
      `_extrair_com_fallback`).
    - `python -c "import ast,sys; src=open('app/main.py',encoding='utf-8').read(); t=ast.parse(src); fn=[n for n in t.body if getattr(n,'name','')=='_extrair_com_fallback']; assert fn, 'helper ausente'; print('ok')"` imprime `ok`.
    - `python -c "import app.main as m; import inspect; s=inspect.getsource(m.gerar_briefings); assert 'escolher_extrator' not in s, 'selecao ainda fora do laco'; print('ok')"` imprime `ok`.
    - Comportamento: com `LLM_API_KEY` exportada e o `LLMExtractor` ainda nao implementado,
      `POST /api/briefings` responde **200** (nao 500) e cada item traz `extrator` igual a
      `heuristico` ou `falha`. Este comportamento e travado automaticamente pelo teste 3 do
      plano 03 — este plano nao precisa escrever o teste.
  </acceptance_criteria>
  <done>
    `escolher_extrator()` roda uma vez por URL dentro do laco (L-02), qualquer excecao de
    extrator degrada para o heuristico sem virar HTTP 500 (L-05), e o degrau final produz
    briefing de falha em vez de propagar excecao.
  </done>
</task>

<task type="auto">
  <name>Task 2: Caminho absoluto para StaticFiles e FileResponse</name>
  <files>app/main.py</files>
  <read_first>
    - `app/main.py` linhas 104 e 109 — estado atual: `StaticFiles(directory="static")` e
      `FileResponse("static/index.html")`, ambos relativos ao working directory.
    - `app/db.py` linhas 15-17 — padrao de import de `pathlib` (`from pathlib import Path`) e de
      constante de caminho de modulo (`DB_PATH`) ja usado no projeto.
    - `.planning/codebase/CONCERNS.md` secao "Non-Absolute Static File Paths" — descreve o
      impacto (UI inacessivel quando o cwd nao e a raiz do projeto).
  </read_first>
  <action>
Em `app/main.py`, elimine a dependencia do working directory nos dois pontos que servem arquivo
estatico.

Adicione `from pathlib import Path` no bloco de imports da biblioteca padrao (antes do bloco de
terceiros, mantendo a ordem descrita em `.planning/codebase/CONVENTIONS.md`).

Declare duas constantes de modulo em UPPER_SNAKE_CASE logo apos a criacao de `app = FastAPI(...)`:

- `BASE_DIR = Path(__file__).resolve().parent.parent` — a raiz do repositorio, um nivel acima do
  pacote `app`.
- `STATIC_DIR = BASE_DIR / "static"`

Troque `app.mount("/static", StaticFiles(directory="static"), name="static")` para usar
`directory=STATIC_DIR`, e `return FileResponse("static/index.html")` para
`return FileResponse(STATIC_DIR / "index.html")`. Mantenha as assinaturas de rota, o `name="static"`
e o tipo de retorno `-> FileResponse` inalterados.

Nao altere `DB_PATH` em `app/db.py` — o caminho relativo do banco esta fora do escopo desta fase
por decisao do usuario.
  </action>
  <verify>
    <automated>python -c "import os,sys,tempfile; root=os.getcwd(); sys.path.insert(0, root); os.chdir(tempfile.mkdtemp()); from fastapi.testclient import TestClient; import app.main as m; c=TestClient(m.app); r=c.get('/'); assert r.status_code==200, r.status_code; assert r.text.lstrip().startswith('<!DOCTYPE html>'), r.text[:40]; print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '"static/index.html"' app/main.py` imprime `0` e
      `grep -c 'directory="static"' app/main.py` imprime `0`.
    - `python -c "import app.main as m; from pathlib import Path; assert m.STATIC_DIR.is_absolute(); assert (m.STATIC_DIR / 'index.html').exists(); print('ok')"` imprime `ok`.
    - Rodando o interpretador a partir de um diretorio temporario (cwd diferente da raiz do
      projeto), `GET /` via `TestClient` devolve status **200** e o corpo comeca com
      `<!DOCTYPE html>`.
    - `pytest -q` continua saindo com codigo 0.
  </acceptance_criteria>
  <done>
    Servir a UI passa a independer do working directory: `BASE_DIR` e `STATIC_DIR` sao absolutos
    e usados tanto no `mount` quanto na rota `/`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| navegador -> API (`POST /api/briefings`) | lista de URLs controlada pelo usuario cruza aqui |
| site de terceiro -> processo da API | HTML nao confiavel entra via `buscar_html` e chega ao extrator |
| processo -> filesystem (`STATIC_DIR`) | caminho de arquivo servido pela rota `/` |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-01 | Denial of Service | `gerar_briefings` laco de URLs | high | mitigate | Task 1: excecao de extrator degrada por URL em tres degraus; um link ruim nao derruba o lote nem vira 500 (L-02, SPEC §6) |
| T-05-02 | Information Disclosure | mensagem de erro devolvida ao vendedor | medium | mitigate | Task 1 degrau 3 reusa o formato curto do `except FetchError`; nenhum stack trace e nenhuma variavel de ambiente entra no `resumo` |
| T-05-03 | Tampering | `FileResponse` / `StaticFiles` com caminho relativo | low | mitigate | Task 2 fixa a raiz em `STATIC_DIR` derivado de `__file__`; a rota `/` nao aceita parametro de caminho, entao nao ha superficie de path traversal |
| T-05-04 | Elevation of Privilege | rotas sem autenticacao | medium | accept | autenticacao e escopo declarado da Fase 6 (SPEC §15); ASVS L1 nao exige controle de acesso em MVP sem dados de usuario |

Nenhum pacote novo e instalado neste plano (L-06), portanto o package-legitimacy gate nao se
aplica; `requirements.txt` permanece intocado.
</threat_model>

<verification>
- `pytest -q` verde, 3 testes.
- `POST /api/briefings` com `LLM_API_KEY` exportada responde 200 em vez de 500.
- `GET /` responde 200 a partir de qualquer working directory.
</verification>

<success_criteria>
- Selecao de extrator acontece por URL, dentro do laco (L-02).
- Qualquer excecao de extrator degrada para o heuristico; se ate o heuristico falhar, o item vira
  briefing de falha com `confianca="baixa"` (L-05, SPEC §6).
- `StaticFiles` e `FileResponse` usam caminho absoluto.
</success_criteria>

## Multi-Source Coverage Audit (conjunto de planos da Fase 5)

Auditoria das quatro fontes antes de fechar o conjunto. Vale para os planos 01 a 07.

### GOAL — `SPEC-sales-intel.md` §15, Fase 5

| Item | Status | Onde |
|---|---|---|
| `LLMExtractor` ligado | COVERED | 05-05 T1, T2 |
| Saida estruturada validada | COVERED | 05-05 T2 (D-02, L-03) |
| Selecao automatica entre LLM e heuristico | COVERED | 05-01 T1 (L-02), 05-03 T1 |
| Tratamento de erro | COVERED | 05-01 T1, 05-05 T2, 05-06 T1 |
| Limite de caracteres | COVERED | 05-04 T1, T3; 05-05 T1 (L-04) |
| Pronto: "sem chave, cai no heuristico sem quebrar" | COVERED | 05-01 T1, 05-03 T1/T2, 05-05 T3 |
| Pronto: "com chave, o briefing vem rico" | COVERED (verificacao `backstop`) | 05-05 — nao verificavel sem chave; ver `must_haves` de 05-05 |

### REQ — IDs de requisito

`phase_req_ids` e **null**: este projeto nao tem `REQUIREMENTS.md` nem `ROADMAP.md`, por decisao
de processo registrada no topo de `05-CONTEXT.md`. Nao ha REQ-ID a rastrear. Os IDs de decisao
travada (**L-01..L-06**, **D-01..D-12**) fazem esse papel e estao no campo `requirements` de cada
plano. Cobertura conferida: os 18 IDs aparecem em pelo menos um plano.
*Spec-less probe fallback skipped: phase has no requirement IDs to probe (visible skip).*

### RESEARCH — nenhum

Nao existe `RESEARCH.md`: o usuario pulou a pesquisa porque as decisoes que ela informaria
(D-01 httpx direto, D-02 `json_schema`, D-03 20s sem retry, D-04 corte de 12k) ja estavam
travadas. Nenhum item de pesquisa a auditar; nenhuma task de pesquisa planejada.

### CONTEXT — `05-CONTEXT.md`

| Item de escopo ("Entra nesta fase") | Status | Onde |
|---|---|---|
| 1. `main.py` escolha de extrator por URL com `try/except` | COVERED | 05-01 T1 |
| 2. `static/index.html` mojibake + BOM | COVERED | 05-02 T2 |
| 3. `fetcher.py` timeout no `robots.txt` | COVERED | 05-02 T1 |
| 4. `main.py` caminho absoluto em `StaticFiles`/`FileResponse` | COVERED | 05-01 T2 |
| 5. `LLMExtractor` com saida estruturada e prompt endurecido | COVERED | 05-05 T1, T2 |
| 6. Visibilidade da degradacao para o vendedor | COVERED | 05-06 T1, T2, T3 |
| 7. Regra de upgrade de extrator no cache | COVERED | 05-07 T1, T2 |
| `config.py` + `.env.example` (D-04, item de pronto da §17) | COVERED | 05-04 T1, T2 |
| D-12: quatro testes offline | COVERED | 05-03 T1 (1 e 2), 05-03 T2 (3), 05-05 T3 (4) |

**Nenhum item MISSING.** Nada foi reduzido, adiado ou simplificado em relacao as fontes.

**Exclusoes legitimas (nao sao lacunas):** os itens listados em `## Deferred Ideas` e em
"Escopo cortado explicitamente" do `05-CONTEXT.md` — `conteudo_hash` (§8), `config.py` com as
oito variaveis (§13), `tests/` em quatro arquivos (§12), `README.md` / `AI-LOG.md` / Docker
(Fase 7), exportar briefing (§4 item 8), conjunto de avaliacao de prompt (§11, precisa de chave),
autenticacao e admin (Fase 6), conexao SQLite nao fechada (`db.py:61`), `@app.on_event`
depreciado (`main.py:28`) e comparacao automatica entre extratores (§7 item 3).

## Artifacts this phase produces

**Produzidos por este plano:**
- `app/main.py::_extrair_com_fallback(url, titulo, texto) -> tuple[Briefing, str]` (novo; vira
  3-tupla no plano 06)
- `app/main.py::BASE_DIR`, `app/main.py::STATIC_DIR` (novas constantes de modulo)

**Demais artefatos da fase (roll-up completo):**
- Plano 02: `app/fetcher.py::ROBOTS_TIMEOUT`; `pode_raspar()` reescrito sobre `httpx`;
  `static/index.html` reescrito em UTF-8 sem BOM e sem duplo encode.
- Plano 03: `test_smoke.py::test_escolher_extrator_sem_chave_devolve_heuristico`,
  `::test_escolher_extrator_com_chave_devolve_llm`,
  `::test_extrator_que_falha_nao_derruba_a_requisicao`.
- Plano 04: `app/config.py` (novo) com `llm_api_key()`, `LLM_MODELO`, `LLM_MAX_CHARS`;
  `.env.example` (novo).
- Plano 05: `app/extractor.py::LLMError`, `::LLM_BASE_URL`, `::LLM_TIMEOUT`,
  `::LLMExtractor._montar_mensagens`, `::LLMExtractor._chamar_provedor`,
  `::LLMExtractor.extrair` (implementado); `test_smoke.py::test_llm_sem_chave_levanta_erro_claro`.
- Plano 06: `app/schemas.py::BriefingResponse.degradado`; `/health` com `llm_disponivel`;
  `static/index.html` classe CSS `.tag-aviso` e a `<span>` condicional em `render()`.
- Plano 07: `app/db.py::buscar(url, llm_disponivel)` com a regra de upgrade de extrator.

<output>
Create `.planning/phases/05-llm/05-01-SUMMARY.md` when done
</output>
