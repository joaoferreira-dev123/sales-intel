---
phase: 05-llm
plan: 03
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - test_smoke.py
autonomous: true
requirements: [D-12, L-02, L-05, L-01]

estimate:
  tokens: 32000
  raw_tokens: 16000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "sem LLM_API_KEY no ambiente, escolher_extrator() devolve HeuristicExtractor (trava L-05)"
    - "com LLM_API_KEY no ambiente, escolher_extrator() devolve LLMExtractor"
    - "um extrator que levanta excecao produz HTTP 200 e o item volta com extrator igual a heuristico, em vez de derrubar o lote"
    - "os tres testes novos rodam sem internet, sem chave de API e sem escrever em briefings.db (SPEC §14)"
  artifacts:
    - "test_smoke.py::test_escolher_extrator_sem_chave_devolve_heuristico"
    - "test_smoke.py::test_escolher_extrator_com_chave_devolve_llm"
    - "test_smoke.py::test_extrator_que_falha_nao_derruba_a_requisicao"
  key_links:
    - "test 3 monkeypatcha app.main.escolher_extrator, app.main.buscar_html, app.db.salvar e app.db.buscar — nenhuma rede, nenhum disco"
    - "test 3 e o teste de regressao do defeito que motiva a fase (main.py capturava apenas FetchError)"
---

<objective>
Cobrir com teste automatizado o caminho que segura tudo o mais: o fallback. Sao os testes 1, 2 e
3 de D-12, todos **sem chave e sem rede** (SPEC §14).

O teste 3 e o teste de regressao do defeito confirmado que motiva a fase — hoje `main.py`
capturava apenas `FetchError`, entao um extrator que levantasse qualquer outra excecao virava
HTTP 500 e derrubava tambem as URLs do lote que ja tinham dado certo.

Purpose: item 2 da tabela `<execution_order>` do CONTEXT.md. **Vem antes do `LLMExtractor` de
proposito** — a garantia estrutural da fase e que o caminho nao verificado (LLM, sem chave para
testar) so consegue falhar *para dentro* do caminho verificado. Este plano e o que torna esse
caminho verificado.
Output: `test_smoke.py` com 6 testes verdes.
</objective>

<planner_notes>
**Teste 4 de D-12 nao entra aqui.** O `<execution_order>` travado diz explicitamente
"Testes 1–3 do fallback" no item 2. O teste 4 (`LLMExtractor.extrair()` sem chave levanta erro
claro) vive no plano 05, junto da reescrita do `LLMExtractor` — que e exatamente o codigo capaz
de quebra-lo.

**O teste 3 evolui no plano 06.** Neste plano ele assevera o que e verdade e estavel apos o
plano 01: HTTP 200 e `extrator == "heuristico"`. O campo `degradado` (D-05) so existe a partir do
plano 06, e aquele plano **estende esta mesma funcao de teste** com a assercao do campo, em vez
de criar um quinto teste — preservando os "quatro testes" de D-12.
</planner_notes>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@SPEC-sales-intel.md
@.planning/phases/05-llm/05-CONTEXT.md
@.planning/phases/05-llm/05-PATTERNS.md
@test_smoke.py
@app/main.py
@app/extractor.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Testes 1 e 2 — escolher_extrator() com e sem chave</name>
  <files>test_smoke.py</files>
  <read_first>
    - `test_smoke.py` — arquivo inteiro (3 testes existentes, 33 linhas). O estilo a replicar:
      funcao `test_*` solta no modulo, sem classe, sem fixture, `assert` direto, constante `HTML`
      de modulo.
    - `app/extractor.py` — `escolher_extrator()` (linhas 92-100), `LLMExtractor.__init__`
      (linha 76-78, le `os.getenv("LLM_API_KEY")` em tempo de chamada) e `disponivel()`.
    - `.planning/codebase/TESTING.md` secoes "Test File Organization" e "Common Patterns".
    - `SPEC-sales-intel.md` §14 — "se o teste precisa de internet para passar, ele esta mal
      escrito".
  </read_first>
  <action>
Acrescente dois testes ao final de `test_smoke.py`, seguindo exatamente o estilo dos tres ja
existentes (funcao solta, sem classe, sem fixture, `assert` direto).

**Imports:** estenda o import ja existente de `app.extractor` para trazer tambem
`LLMExtractor` e `escolher_extrator`, mantendo ordem alfabetica:
`from app.extractor import HeuristicExtractor, LLMExtractor, escolher_extrator`.

**Teste 1 — `test_escolher_extrator_sem_chave_devolve_heuristico(monkeypatch)`:**
usa `monkeypatch.delenv("LLM_API_KEY", raising=False)` e assevera
`isinstance(escolher_extrator(), HeuristicExtractor)`. Este e o teste que trava L-05: sem chave,
o sistema roda no heuristico. Deixe um comentario de uma linha dizendo isso.

**Teste 2 — `test_escolher_extrator_com_chave_devolve_llm(monkeypatch)`:**
usa `monkeypatch.setenv("LLM_API_KEY", "chave-de-teste-sem-valor")` e assevera
`isinstance(escolher_extrator(), LLMExtractor)`. O valor da variavel e um literal obviamente
falso; nao use nada que pareca uma credencial real.

O parametro `monkeypatch` entra como argumento da funcao de teste (padrao pytest idiomatico);
nao importe nada de `unittest.mock`.

**Restricao de projeto que estes dois testes impoem ao resto da fase:** eles monkeypatcham a
**variavel de ambiente**, nao um atributo de modulo. Isso exige que a leitura de `LLM_API_KEY`
continue acontecendo **em tempo de chamada** e nao congelada no import — e a razao pela qual o
plano 04 expoe a chave por meio da funcao `config.llm_api_key()` em vez de uma constante de
modulo. Nao mude estes testes quando `app/config.py` chegar.
  </action>
  <verify>
    <automated>pytest -q test_smoke.py::test_escolher_extrator_sem_chave_devolve_heuristico test_smoke.py::test_escolher_extrator_com_chave_devolve_llm</automated>
  </verify>
  <acceptance_criteria>
    - `pytest -q test_smoke.py -k escolher_extrator` sai com codigo 0 e reporta 2 testes passando.
    - `pytest -q` sai com codigo 0 e reporta 5 testes no total.
    - Os dois testes passam com a variavel `LLM_API_KEY` **presente** no ambiente do
      desenvolvedor e tambem com ela ausente:
      `LLM_API_KEY=qualquer-coisa pytest -q -k escolher_extrator` sai com codigo 0.
    - `grep -c 'unittest.mock' test_smoke.py || true` imprime `0`.
    - `grep -c 'import pytest' test_smoke.py || true` imprime `0` neste ponto (o `pytest.raises`
      so chega no plano 05, com o teste 4).
  </acceptance_criteria>
  <done>
    A escolha automatica entre LLM e heuristico esta travada por teste nos dois estados, e roda
    offline.
  </done>
</task>

<task type="auto">
  <name>Task 2: Teste 3 — extrator que falha nao derruba a requisicao</name>
  <files>test_smoke.py</files>
  <read_first>
    - `test_smoke.py` — estado apos a Task 1, incluindo a constante `HTML` de modulo que sera
      reaproveitada como corpo de pagina falso.
    - `app/main.py` — estado apos o plano 01: `_extrair_com_fallback` e o laco de
      `gerar_briefings`. Confirme os nomes de modulo que serao monkeypatchados
      (`escolher_extrator` e `buscar_html` sao importados para dentro de `app.main`).
    - `app/db.py` — assinaturas de `buscar()` e `salvar()`, para escrever substitutos com
      assinatura tolerante.
    - `.planning/codebase/TESTING.md` secao "Future Testing Patterns / For API Endpoints" — o
      unico ponto desta fase sem analog no repositorio; e o padrao `TestClient` do FastAPI.
    - `.planning/codebase/CONCERNS.md` secao "Untested: Extractor Exception Handling", marcada
      como prioridade alta e bloqueadora de demo.
  </read_first>
  <action>
Acrescente ao final de `test_smoke.py` o teste de regressao do defeito critico:
`test_extrator_que_falha_nao_derruba_a_requisicao(monkeypatch)`.

**Imports novos no topo do arquivo:** `from datetime import datetime, timezone`,
`from fastapi.testclient import TestClient`, `from app import db, main`. Mantenha os imports da
biblioteca padrao, de terceiros e locais em blocos separados, na ordem descrita em
`.planning/codebase/CONVENTIONS.md`.

**Dentro do teste, defina uma classe local minima** que cumpre o `Extractor` Protocol e sempre
falha:

- atributo de classe `nome = "llm"`
- metodo `extrair(self, url, titulo, texto)` que levanta `RuntimeError("provedor fora do ar")`

**Quatro monkeypatches, todos obrigatorios** — o teste nao pode tocar rede nem disco (SPEC §14):

- `monkeypatch.setattr(main, "escolher_extrator", lambda: <instancia da classe local>)` — forca o
  caminho de falha.
- `monkeypatch.setattr(main, "buscar_html", lambda url: HTML)` — reaproveita a constante `HTML`
  ja existente no arquivo, sem rede.
- `monkeypatch.setattr(db, "salvar", lambda *args, **kwargs: datetime.now(timezone.utc))` — sem
  escrita em `briefings.db`. Assinatura tolerante de proposito.
- `monkeypatch.setattr(db, "buscar", lambda *args, **kwargs: None)` — sem leitura de cache.
  Assinatura tolerante **de proposito**: o plano 07 acrescenta o parametro `llm_disponivel` a
  `db.buscar()`, e `*args, **kwargs` faz este teste sobreviver aquela mudanca sem edicao.

**Cliente e requisicao:** instancie `TestClient(main.app)` **sem** usar o `with` como
context manager. Isso e deliberado: sem o context manager o ciclo de lifespan nao roda, entao o
hook de startup nao chama `db.criar_tabelas()` e o teste nao cria arquivo de banco. Deixe um
comentario de uma linha explicando isso.

Faca `client.post("/api/briefings", json={"urls": ["https://acme.com.br"],
"forcar_atualizacao": True})`.

**Assercoes:**
- `resp.status_code == 200` — esta e a assercao central: o defeito produzia 500.
- a resposta e uma lista com exatamente 1 item.
- `item["extrator"] == "heuristico"` — degradou em vez de estourar, e nao virou `"falha"`.
- `item["briefing"]["confianca"] == "baixa"` — o heuristico e honesto sobre o que entrega.
- `item["origem"] == "novo"`.

Nao assevere nada sobre um campo `degradado` neste plano: esse campo so nasce no plano 06, que
estende **esta mesma funcao** com a assercao correspondente.
  </action>
  <verify>
    <automated>pytest -q test_smoke.py::test_extrator_que_falha_nao_derruba_a_requisicao</automated>
  </verify>
  <acceptance_criteria>
    - `pytest -q` sai com codigo 0 e reporta **6** testes passando.
    - O teste passa com a rede desligada e sem `LLM_API_KEY` no ambiente.
    - O teste nao cria nem modifica `briefings.db`: rodar
      `pytest -q test_smoke.py::test_extrator_que_falha_nao_derruba_a_requisicao` num diretorio
      limpo nao produz arquivo `.db` novo.
    - `grep -c 'TestClient' test_smoke.py` imprime `2` (o import e a instanciacao).
    - `grep -c 'with TestClient' test_smoke.py || true` imprime `0` (sem context manager, para
      nao disparar o lifespan).
    - Regressao demonstrada: revertendo mentalmente o plano 01 (extrator escolhido fora do laco e
      apenas `except FetchError`), este teste falharia com status 500 — ele existe exatamente
      para travar essa regressao.
  </acceptance_criteria>
  <done>
    O caminho de degradacao por URL esta coberto por teste automatizado, offline, e o defeito que
    motiva a fase nao pode voltar sem quebrar a suite.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| suite de testes -> ambiente do desenvolvedor | `monkeypatch` de `LLM_API_KEY` toca variavel que em producao guarda segredo |
| suite de testes -> rede / disco | o que a SPEC §14 proibe atravessar |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-10 | Information Disclosure | valor usado no `monkeypatch.setenv("LLM_API_KEY", ...)` | high | mitigate | Task 1 fixa o literal `chave-de-teste-sem-valor`; nenhuma credencial real entra no repositorio (SPEC §6, §17) |
| T-05-11 | Information Disclosure | `LLM_API_KEY` real vazando do ambiente para o teste | medium | mitigate | teste 1 usa `delenv` e teste 2 usa `setenv`; nenhum teste imprime ou assevera o valor da variavel |
| T-05-12 | Denial of Service | teste que toca rede pendura a suite | medium | mitigate | Task 2 monkeypatcha `buscar_html`; criterio de aceite exige passar com a rede desligada |
| T-05-13 | Tampering | teste escrevendo em `briefings.db` de producao local | medium | mitigate | Task 2 monkeypatcha `db.salvar` e `db.buscar` e instancia `TestClient` sem context manager, evitando `criar_tabelas()` |
| T-05-14 | Repudiation | ausencia de teste do caminho de falha | high | mitigate | este plano inteiro; o defeito so era invisivel porque nao havia teste (`.planning/codebase/CONCERNS.md`, prioridade alta) |

Nenhum pacote novo e instalado neste plano: `pytest`, `fastapi` e `httpx` (dependencia do
`TestClient`) ja constam do `requirements.txt` (L-06). O package-legitimacy gate nao se aplica.
</threat_model>

<verification>
- `pytest -q` verde com 6 testes.
- Suite roda offline, sem chave e sem escrever em banco.
</verification>

<success_criteria>
- Testes 1, 2 e 3 de D-12 existem, passam e cobrem L-05, L-01 e o defeito de L-02.
- A suite continua sem internet, sem mock library e sem fixture (SPEC §14).
</success_criteria>

## Artifacts this phase produces

**Produzidos por este plano:**
- `test_smoke.py::test_escolher_extrator_sem_chave_devolve_heuristico`
- `test_smoke.py::test_escolher_extrator_com_chave_devolve_llm`
- `test_smoke.py::test_extrator_que_falha_nao_derruba_a_requisicao`

**Demais artefatos da fase (roll-up completo):**
- Plano 01: `app/main.py::_extrair_com_fallback`, `::BASE_DIR`, `::STATIC_DIR`.
- Plano 02: `app/fetcher.py::ROBOTS_TIMEOUT`; `pode_raspar()` reescrito; `static/index.html` em
  UTF-8 sem BOM.
- Plano 04: `app/config.py` (novo) com `llm_api_key()`, `LLM_MODELO`, `LLM_MAX_CHARS`;
  `.env.example` (novo).
- Plano 05: `app/extractor.py::LLMError`, `::LLM_BASE_URL`, `::LLM_TIMEOUT`,
  `::LLMExtractor._montar_mensagens`, `::LLMExtractor._chamar_provedor`,
  `::LLMExtractor.extrair`; `test_smoke.py::test_llm_sem_chave_levanta_erro_claro`.
- Plano 06: `app/schemas.py::BriefingResponse.degradado`; `/health` com `llm_disponivel`;
  `static/index.html` classe `.tag-aviso` e `<span>` condicional em `render()`.
- Plano 07: `app/db.py::buscar(url, llm_disponivel)` com a regra de upgrade de extrator.

<output>
Create `.planning/phases/05-llm/05-03-SUMMARY.md` when done
</output>
