---
phase: 05-llm
plan: 09
type: execute
wave: 2
depends_on: ["05-08"]
gap_closure: true
files_modified:
  - app/schemas.py
  - test_smoke.py
autonomous: true
requirements: [L-02, L-03, L-06, D-01, D-02, D-03, D-05, D-09, D-10, D-11, D-12]

estimate:
  tokens: 52000
  raw_tokens: 26000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "o campo extrator de BriefingResponse so aceita llm, heuristico ou falha; um valor fora da enumeracao vindo de linha antiga do cache vira miss e forca recoleta, sem virar 500 (SPEC S8, D-05, L-02)"
    - "a regra de upgrade de extrator do cache esta travada nas duas direcoes: linha heuristico com LLM disponivel vira miss, e linha llm sem LLM disponivel continua sendo servida (D-09)"
    - "uma pagina que imprime o proprio delimitador nao consegue fechar o bloco de dado nao confiavel: o conteudo nao confiavel da mensagem de usuario contem exatamente um delimitador de abertura e um de fechamento (D-11)"
    - "o galho de degradacao de saida estruturada faz exatamente uma segunda chamada ao provedor e nunca entra em laco: um segundo 400 vira LLMError (D-02, D-03)"
    - "pytest -q roda offline, sem chave de API e sem rede, com 16 testes verdes; nenhum teste abre socket nem toca briefings.db (D-12)"
  artifacts:
    - "app/schemas.py::BriefingResponse.extrator (tipo fechado em Literal)"
    - "test_smoke.py::test_cache_com_extrator_fora_da_enumeracao_vira_miss"
    - "test_smoke.py::test_cache_heuristico_vira_miss_quando_llm_disponivel"
    - "test_smoke.py::test_cache_llm_sobrevive_quando_llm_indisponivel"
    - "test_smoke.py::test_montar_mensagens_remove_delimitador_forjado"
    - "test_smoke.py::test_degradacao_json_schema_faz_exatamente_uma_segunda_chamada"
    - "test_smoke.py::test_segundo_400_de_json_schema_vira_llmerror"
  key_links:
    - "a guarda de cache criada em 05-08 T2 e o que torna seguro fechar a enumeracao de extrator: linha antiga fora da enumeracao vira miss, nao 500"
    - "os testes de D-09 monkeypatcham db.DB_PATH para tmp_path; briefings.db real nunca e aberto nem alterado (D-10)"
    - "os testes de D-02 dublam httpx.Client dentro do namespace de app.extractor; nenhum socket e aberto e a chave vem por argumento de construtor, nunca do ambiente (D-12)"
---

<objective>
Fechar os três achados de warning que a `05-VERIFICATION.md` confirmou como ainda válidos e que o
`05-REVIEW.md` levantou — `WR-01` e `WR-02`. (`WR-03` foi fechado no plano 05-08, junto do blocker,
por morar no mesmo arquivo e sob o mesmo contrato D-06.)

Nenhum deles bloqueia o objetivo da fase, e a verificação diz isso explicitamente. O que eles
representam é dívida concentrada exatamente nos comportamentos que o `05-CONTEXT.md` marca como
os mais importantes de acertar:

- **WR-01** — `schemas.py:58` declara `extrator: str`. A SPEC §8 exige a enumeração fechada
  `llm` / `heuristico` / `falha`, e todo o desenho de `_extrair_com_fallback` (D-05) existe para
  nunca introduzir um quarto valor. Hoje isso é convenção, não invariante — duas linhas abaixo,
  `Briefing.confianca` já mostra a forma certa com `Literal`.
- **WR-02** — as três rotas mais carregadas da fase (regra de upgrade de cache **D-09**, galho de
  degradação de saída estruturada **D-02**, anti-forja de delimitador **D-11**) foram exercitadas
  apenas por scripts ad-hoc durante a execução. Esses scripts não existem mais. Uma regressão em
  qualquer uma delas — um `or` virando `and` no predicado de cache, a segunda chamada ao provedor
  virando alcançável mais de uma vez, o `replace` de anti-forja sumindo num refactor — hoje
  embarcaria sem nada quebrar.

Purpose: fechar `WR-01` e `WR-02` de `05-REVIEW.md`, confirmados na tabela "Anti-Patterns Found"
da `05-VERIFICATION.md`.
Output: `app/schemas.py` com a enumeração fechada; `test_smoke.py` com 6 testes novos (10 → 16),
todos sem chave e sem rede.
</objective>

<planner_notes>
**Por que este plano existe separado do 05-08, e por que ele existe.** O blocker (`L-02`) é uma
correção apertada e de risco baixo; enchê-lo de trabalho adjacente teria custo próprio. Por isso a
batelada foi partida: **o 05-08 sela o blocker sozinho na onda 1**. Este plano é deliberadamente
severável — se a batelada precisar encolher, ele sai inteiro sem tocar no fechamento da lacuna.

Ele **não** sai por padrão, porém, e a razão é concreta: o argumento de "dívida de teste apenas"
subestima o caso. As três rotas do WR-02 são todas testáveis **offline, sem chave e sem rede**, em
poucas linhas, e são justamente as que a `05-VERIFICATION.md` aponta como as de maior alavancagem
da fase. O projeto segue para as Fases 6 e 7; uma regressão silenciosa no predicado de cache ou na
anti-forja de delimitador é exatamente o tipo de coisa que só aparece na frente de um cliente.

**Dependência real, não decorativa, entre WR-01 e o plano 05-08.** Fechar `extrator` num `Literal`
**introduz** uma rota de falha nova: uma linha de `briefings.db` gravada com valor fora da
enumeração passa a levantar `ValidationError` na montagem de `BriefingResponse`. Sem a guarda de
cache criada em 05-08 T2, isso seria um 500 — trocar um warning por um blocker. Com a guarda no
lugar, vira miss e recoleta. Por isso `depends_on: ["05-08"]` e a Task 1 traz o teste que prova
essa interação de ponta a ponta: **o teste do WR-01 é, ao mesmo tempo, a prova de que a guarda do
05-08 está no lugar certo.**

**O que este plano NÃO fecha, e não finge fechar.** A Task 3 exercita o galho `_SemJsonSchema` com
duplo. Isso **não** é a mesma coisa que exercitá-lo contra o Groq real — `gpt-oss-120b` não produz
o HTTP 400 que dispara o galho, e essa limitação está corretamente registrada em `COVERAGE.md`,
no `05-05-SUMMARY.md` e no `05-CONTEXT.md` (D-14). A `05-VERIFICATION.md` classifica esse limite
como **corretamente caracterizado, não como lacuna**. Este plano trava a **lógica** do galho
(quantas chamadas, com qual corpo, e que um segundo 400 não vira laço); o limite de verificação
contra provedor real permanece registrado como risco residual, intocado. O mesmo vale para a
latência por URL, que segue com estimativa de ordem de grandeza.

**Probe spec-less: pulado, visivelmente.** *Spec-less probe fallback skipped: phase has no
requirement IDs to probe (visible skip).* Sem `SPEC` de fase e sem `REQUIREMENTS.md`, não há
`## Edge Coverage` nem `## Prohibitions` a levantar. Os `must_haves` vêm das decisões travadas
`D-02`, `D-03`, `D-09`, `D-11`, `D-12`, da SPEC §8 e dos achados `WR-01`/`WR-02`.

**Nyquist / Dimensão 8:** `05-VALIDATION.md` não existe (o modo `--gaps` pula a pesquisa por
desenho); o usuário optou explicitamente por seguir sem ele. Ausência registrada, não suprida com
estratégia inventada. Toda task tem `<verify><automated>` executável.

**api-coverage: sem mudança.** `COVERAGE.md` desta fase permanece **correto e intocado**. A Task 3
dubla `httpx.Client` para exercitar código já escrito — nenhuma capacidade do Groq entra ou sai da
matriz, e a linha "Limitação registrada" continua exata (ver parágrafo acima). Decisão registrada
em vez de matriz regerada.

**assumption-delta: `no-change`.** Detector não dispara (sem `ROADMAP.md`). Confirmado pela
leitura: fechar `str` em `Literal` **estreita a aplicação** de uma enumeração que já existia na
SPEC §8 — não é uma transição singular→plural, obrigatório→opcional nem derivado→escolhido.
O substantivo primário (a **URL**) não muda.

**schema-gate: não se aplica.** Nenhum ORM detectado; SQLite via `sqlite3` direto. Nenhum comando
de push a injetar. **D-10 proíbe DDL nesta fase**, e a Task 2 opera sobre banco temporário em
`tmp_path` justamente para nunca tocar `briefings.db`.

**Reversibilidade:** T1 é `reversible` — uma anotação de tipo, um arquivo, `Literal` já importado,
nenhum consumidor precisa mudar (a UI só lê o valor). Ela tem consequência de runtime — linhas de
cache fora da enumeração passam a ser recoletadas —, mas é recuperável e não reescreve dado. T2 e
T3 são aditivas. **Nenhum `checkpoint:decision` é justificado**; `REVERSIBILITY_GATES=true` está
ligado e nenhuma decisão desta batelada é `one-way`.
</planner_notes>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@SPEC-sales-intel.md
@.planning/phases/05-llm/05-CONTEXT.md
@.planning/phases/05-llm/05-REVIEW.md
@.planning/phases/05-llm/05-VERIFICATION.md
@.planning/phases/05-llm/05-08-PLAN.md
@app/schemas.py
@app/db.py
@app/extractor.py
@test_smoke.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fechar a enumeracao de extrator em Literal (WR-01)</name>
  <files>app/schemas.py, test_smoke.py</files>
  <read_first>
    - `app/schemas.py` — **arquivo sendo modificado, leia o estado atual inteiro**. A linha 58
      (`extrator: str = Field(...)`) é o alvo; `Briefing.confianca` (linhas 44-47) é o **analog
      exato** de como o projeto declara enumeração fechada; a linha 12 mostra que `Literal` já
      está importado — nenhum import novo é necessário.
    - `app/main.py` — **depois do plano 05-08**. Confirme os quatro pontos que atribuem valor ao
      campo `extrator`, para provar que nenhum deles pode produzir um quarto valor: o retorno de
      `_extrair_com_fallback` (que devolve `extrator.nome`, `"heuristico"` ou `"falha"`), o braço
      `except FetchError`, o braço `except Exception` e o caminho de cache (que usa
      `row["extrator"]` — este é o único não controlado por nós, e é o que a guarda de 05-08 T2
      protege).
    - `app/extractor.py` — atributos `nome` de `HeuristicExtractor` (linha 68) e `LLMExtractor`
      (linha 94): confirme que valem exatamente `heuristico` e `llm`.
    - `app/db.py` — `buscar()` (linhas 41-63), para entender de onde vem o `extrator` de uma linha
      antiga.
    - `test_smoke.py` — **arquivo sendo modificado**; o teste
      `test_cache_incompativel_com_o_schema_vira_miss`, escrito em 05-08 T2, é o molde direto.
    - `.planning/phases/05-llm/05-REVIEW.md` — `WR-01`.
    - `SPEC-sales-intel.md` §8 — a enumeração que está sendo tornada exigível.
  </read_first>
  <behavior>
    - `BriefingResponse` construído com `extrator` igual a `llm`, `heuristico` ou `falha`:
      continua funcionando exatamente como hoje.
    - `BriefingResponse` construído com qualquer outro valor: levanta `ValidationError`.
    - Uma linha de cache cujo `extrator` está fora da enumeração (gravada por um refactor futuro
      ou por versão anterior) **vira miss** e força recoleta — 200, `origem: novo` —, em vez de
      derrubar a requisição. É a guarda de 05-08 T2 fazendo seu trabalho.
  </behavior>
  <action>
Teste vermelho primeiro, correção depois.

**Metade 1 — teste em `test_smoke.py`.**

Acrescente `test_cache_com_extrator_fora_da_enumeracao_vira_miss(monkeypatch)`, copiando a
estrutura de `test_cache_incompativel_com_o_schema_vira_miss` (escrito em 05-08 T2). A diferença é
o que torna a linha de cache inválida: aqui o **dicionário de briefing é perfeitamente válido**
(inclui `empresa` e `resumo`), e o que está fora da enumeração é o **extrator**.

- `monkeypatch.delenv("LLM_API_KEY", raising=False)` — obrigatório (offline, sem chave, D-12).
- `db.buscar` monkeypatchado para devolver a tupla de 3 elementos: o dicionário
  `{"empresa": "Antiga SA", "resumo": "briefing gravado por um refactor futuro"}`, a string
  `gpt-5-turbo` como extrator, e `datetime.now(timezone.utc)`.
  O valor `gpt-5-turbo` é escolhido de propósito: **não** é `heuristico`, então a regra de upgrade
  de D-09 não transforma essa linha em miss por conta própria — o miss precisa vir da validação da
  enumeração, e é isso que o teste isola.
- `main.buscar_html` devolve `HTML`; `db.salvar` devolve `datetime.now(timezone.utc)`.
- Corpo com **uma** URL e `forcar_atualizacao` ausente ou `false`.
- `TestClient(main.app)` sem `with`.

Asserções: status 200; `origem == "novo"`; `extrator == "heuristico"`;
`briefing["empresa"] == "Acme Tecnologia"` — veio da extração fresca, não da linha antiga (cujo
`empresa` era `Antiga SA`, o que torna a asserção discriminante em vez de decorativa).

Rode `pytest -q` e confirme que o teste novo **falha** antes da metade 2: com `extrator: str`, a
linha antiga é aceita e a resposta sai com `origem == "cache"`.

**Metade 2 — correção em `app/schemas.py`.**

Na linha 58, troque a anotação `str` do campo `extrator` de `BriefingResponse` por
`Literal["llm", "heuristico", "falha"]`, mantendo o `Field(...)` e ajustando o texto de
`description` para citar os três valores (o texto atual menciona apenas dois). Siga a forma exata
de `Briefing.confianca` duas declarações abaixo — mesma quebra de linha, mesmo estilo de
argumento. `Literal` já está importado na linha 12; **não** adicione import.

Preserve a codificação do arquivo: `app/schemas.py` começa com BOM UTF-8. Edite sem convertê-la,
para que o diff mostre só a linha alterada.

Não toque em `app/main.py`, `app/db.py` nem `app/extractor.py` nesta task: nenhum deles precisa
mudar, e provar isso pelo diff vazio é parte do critério de aceite — é a evidência de que a
enumeração já era respeitada na prática e só faltava torná-la exigível.
  </action>
  <verify>
    <automated>pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest -q` sai com código 0 e reporta **11 testes passando**. Equivalente com o
      interpretador do projeto: `.venv/Scripts/python.exe -m pytest -q`.
    - `pytest -q -k extrator_fora_da_enumeracao` sai com código 0 e reporta 1 teste passando.
    - `python -c "from app.schemas import BriefingResponse; import typing; a=BriefingResponse.model_fields['extrator'].annotation; assert typing.get_args(a)==('llm','heuristico','falha'), typing.get_args(a); print('ok')"` imprime `ok`.
    - `python -c "import pytest; from datetime import datetime,timezone; from app.schemas import Briefing,BriefingResponse; from pydantic import ValidationError; b=Briefing(empresa='x',resumo='y'); pytest.raises(ValidationError, BriefingResponse, url='u', briefing=b, origem='novo', extrator='gpt-5-turbo', coletado_em=datetime.now(timezone.utc)); print('ok')"` imprime `ok`
      (hoje, antes da correção, esse mesmo comando falha: `BriefingResponse` aceita `gpt-5-turbo`
      sem reclamar — confirmado no planejamento).
    - Comportamento: `POST /api/briefings` com `db.buscar` devolvendo uma linha de extrator
      `gpt-5-turbo` responde **200** com `origem == "novo"` e `extrator == "heuristico"` — não 500,
      e não `origem == "cache"`.
    - `git diff -- app/main.py app/db.py app/extractor.py requirements.txt` não imprime nada.
    - `python -c "print(open('app/schemas.py','rb').read()[:3] == b'\xef\xbb\xbf')"` imprime `True`
      (codificação preservada).
  </acceptance_criteria>
  <reversibility rating="reversible">Uma anotação de tipo num arquivo; `Literal` já importado, nenhum consumidor muda (a UI só lê o campo). Consequência de runtime — linha de cache fora da enumeração passa a ser recoletada — é recuperável e não reescreve dado.</reversibility>
  <done>
    A enumeração da SPEC §8 deixa de ser convenção e vira invariante exigível pelo Pydantic, sem
    abrir rota de 500: linha antiga fora da enumeração vira miss graças à guarda do plano 05-08.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Travar a regra de upgrade de cache (D-09) e a anti-forja de delimitador (D-11)</name>
  <files>test_smoke.py</files>
  <read_first>
    - `app/db.py` — `DB_PATH` (linha 17), `conectar()` (linhas 21-24), `criar_tabelas()`
      (linhas 27-38), `buscar()` (linhas 41-63) e `salvar()` (linhas 66-80). O ponto que importa:
      `conectar()` lê `DB_PATH` **no momento da chamada**, o que torna o monkeypatch do módulo
      suficiente para desviar o banco inteiro para um arquivo temporário.
    - `app/extractor.py` — `DELIM_INICIO` e `DELIM_FIM` (linhas 49-50), e `_montar_mensagens`
      (linhas 103-156): o corte de L-04 na linha 116, a anti-forja nas linhas 118-122, e a forma
      exata da lista devolvida (índice 0 é a mensagem de sistema, índice 1 é a de usuário).
    - `test_smoke.py` — **arquivo sendo modificado**; leia o estado atual completo, incluindo os
      testes adicionados pelo plano 05-08, para manter o estilo (função `test_*` solta, sem
      classe, sem fixture, `assert` direto) e para não colidir com nomes já usados.
    - `.planning/phases/05-llm/05-REVIEW.md` — `WR-02`, que enumera nominalmente as três lacunas
      de cobertura e a forma de fechamento sugerida.
    - `.planning/codebase/TESTING.md` — seções "Test File Organization" e "Common Patterns".
  </read_first>
  <behavior>
    - Linha gravada com extrator `heuristico`, consultada com `llm_disponivel=True`: `db.buscar`
      devolve `None` (miss forçado, para subir a qualidade — D-09).
    - Linha gravada com extrator `llm`, consultada com `llm_disponivel=False`: `db.buscar` devolve
      a tupla normalmente — a regra de upgrade **não** invalida trabalho bom quando a chave some.
    - Uma página cujo texto imprime `DELIM_INICIO` e `DELIM_FIM` literais: a mensagem de usuário
      montada contém **exatamente um** de cada, os do próprio invólucro. O texto forjado
      sobrevive como conteúdo, apenas sem os delimitadores.
    - Um título que imprime um delimitador recebe o mesmo tratamento.
  </behavior>
  <action>
Três testes novos em `test_smoke.py`, todos offline. Nenhuma mudança em código de produção nesta
task.

**(a) `test_cache_heuristico_vira_miss_quando_llm_disponivel(monkeypatch, tmp_path)`** — D-09,
direção "upgrade".

`monkeypatch.setattr(db, "DB_PATH", tmp_path / "teste.db")` **antes de qualquer chamada** ao
módulo. Isso é o que garante que `briefings.db` real nunca é aberto (D-10: nenhuma DDL e nenhuma
escrita no banco do projeto). Em seguida: `db.criar_tabelas()`, depois
`db.salvar(url, {"empresa": "Acme", "resumo": "texto"}, "heuristico")` para uma URL de teste.

Asserções: `db.buscar(url, llm_disponivel=True)` devolve `None` (a linha existe e está fresca, mas
o LLM disponível a invalida); e `db.buscar(url, llm_disponivel=False)` devolve uma tupla cujo
segundo elemento é `heuristico` — as duas juntas provam que o miss veio **da regra de upgrade** e
não de ausência ou vencimento da linha. Essa segunda asserção é o que impede o teste de passar por
acidente se um refactor quebrar a busca inteira.

**(b) `test_cache_llm_sobrevive_quando_llm_indisponivel(monkeypatch, tmp_path)`** — D-09, direção
oposta, pedida nominalmente por WR-02.

Mesmo preparo de `DB_PATH` e `criar_tabelas()`. Grave uma linha com extrator `llm`. Asserção:
`db.buscar(url, llm_disponivel=False)` devolve uma tupla cujo segundo elemento é `llm` — uma
entrada boa não é invalidada só porque a chave saiu do ambiente. Este é o teste que pega o
`or`↔`and` invertido no predicado.

**(c) `test_montar_mensagens_remove_delimitador_forjado()`** — D-11, terceira camada da mitigação
de injeção.

`_montar_mensagens` é função pura: não abre rede, e a chave entra por argumento de construtor.
Instancie `LLMExtractor(api_key="chave-de-teste-sem-valor")` — assim o teste não lê `LLM_API_KEY`
do ambiente e não precisa de `monkeypatch`.

Monte um texto de página que tenta forjar o invólucro: um trecho reconhecível, seguido de
`extractor.DELIM_FIM` literal, seguido de uma instrução falsa reconhecível, seguido de
`extractor.DELIM_INICIO` literal, seguido de outro trecho reconhecível. Passe também um título
que contenha `extractor.DELIM_FIM` literal. Importe os dois delimitadores do módulo em vez de
reescrever as strings à mão — reescrevê-las faria o teste passar a testar a cópia, não o código.

Pegue `conteudo = mensagens[1]["content"]` (a mensagem de papel `user`) e afirme:
`conteudo.count(DELIM_INICIO) == 1` e `conteudo.count(DELIM_FIM) == 1` — restaram apenas os do
invólucro legítimo. As contagens são inequívocas porque nenhum dos dois delimitadores é substring
do outro (`DELIM_FIM` tem o segmento `FIM_` logo após a abertura). Afirme também que os trechos
reconhecíveis e a instrução falsa **continuam presentes** no conteúdo: o objetivo de D-11 é
neutralizar o delimitador, não censurar a página. Por fim, afirme que o conteúdo do título limpo
aparece na mensagem e que o delimitador que ele carregava não sobreviveu — reaproveite as mesmas
contagens.

Escopo as asserções de contagem à **mensagem de índice 1**: a mensagem de sistema (índice 0) cita
os dois delimitadores pelo nome de propósito, ao instruir o modelo, e contá-los ali daria falso
negativo.
  </action>
  <verify>
    <automated>pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest -q` sai com código 0 e reporta **14 testes passando** em menos de 3s.
    - `pytest -q -k "cache_heuristico or cache_llm_sobrevive or delimitador_forjado"` sai com
      código 0 e reporta 3 testes passando.
    - Comportamento (D-09): com `DB_PATH` desviado para `tmp_path`, uma linha `heuristico` fresca
      consultada com `llm_disponivel=True` devolve `None`, e a mesma linha consultada com
      `llm_disponivel=False` devolve tupla com segundo elemento `heuristico`.
    - Comportamento (D-09): uma linha `llm` fresca consultada com `llm_disponivel=False` devolve
      tupla com segundo elemento `llm`.
    - Comportamento (D-11): para um texto e um título que imprimem os delimitadores literais,
      `mensagens[1]["content"]` contém exatamente 1 ocorrência de `DELIM_INICIO` e exatamente 1 de
      `DELIM_FIM`, e os trechos de conteúdo forjado continuam presentes.
    - `git status --porcelain briefings.db` não imprime nada — o banco do projeto não foi criado
      nem alterado pela suíte (D-10).
    - `git diff --stat -- app/ requirements.txt` não imprime nada (task só de teste).
    - `python -c "import ast; t=ast.parse(open('test_smoke.py',encoding='utf-8-sig').read()); ns={n.name for n in t.body if isinstance(n,ast.FunctionDef)}; assert {'test_cache_heuristico_vira_miss_quando_llm_disponivel','test_cache_llm_sobrevive_quando_llm_indisponivel','test_montar_mensagens_remove_delimitador_forjado'} <= ns; print('ok')"` imprime `ok`.
      (`utf-8-sig` é obrigatório aqui: `test_smoke.py` tem BOM UTF-8, e `utf-8` puro faria
      `ast.parse` morrer com `SyntaxError` em vez de avaliar o critério. Preserve o BOM ao editar.)
  </acceptance_criteria>
  <reversibility rating="reversible">Testes aditivos em um arquivo; nenhum código de produção muda e nenhum estado persiste (banco em `tmp_path`).</reversibility>
  <done>
    A regra de upgrade de cache está travada nas duas direções e a anti-forja de delimitador tem
    teste persistido. Uma regressão em qualquer das duas passa a quebrar a suíte em vez de
    embarcar em silêncio.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Travar o galho de degradacao de saida estruturada (D-02) com httpx dublado</name>
  <files>test_smoke.py</files>
  <read_first>
    - `app/extractor.py` — **leitura central desta task**: `_SemJsonSchema` (linhas 39-41),
      `LLM_TIMEOUT` (linha 47), `_chamar_provedor` (linhas 158-213) — em especial a montagem de
      `corpo` (linhas 164-182), o uso de `httpx.Client` como context manager (linhas 184-192), a
      condição de disparo do galho (linhas 198-205: status 400 **e** `usar_json_schema` **e** o
      corpo da resposta citando o formato) e a leitura de `choices[0].message.content`
      (linha 211) —, e `extrair` (linhas 215-238), onde vive a **única** segunda chamada permitida
      (linhas 221-226).
    - `app/config.py` — `LLM_BASE_URL` e `LLM_MODELO`, para saber o que o duplo vai receber sem
      precisar de ambiente configurado.
    - `test_smoke.py` — **arquivo sendo modificado**; estado atual completo, incluindo os testes
      das tasks anteriores, para manter estilo e não colidir com nomes.
    - `.planning/phases/05-llm/05-REVIEW.md` — `WR-02`, terceiro item.
    - `.planning/phases/05-llm/05-CONTEXT.md` — D-02 (cinto e suspensório) e D-03 (sem retry): são
      as duas decisões que estes testes tornam exigíveis.
    - `.planning/phases/05-llm/COVERAGE.md` — seção "Limitação registrada": leia para não
      alegar mais do que este teste prova.
  </read_first>
  <behavior>
    - Provedor devolvendo 400 citando o formato estruturado na primeira chamada e 200 com JSON
      válido na segunda: `LLMExtractor.extrair` devolve um `Briefing` válido, tendo feito
      **exatamente duas** chamadas.
    - O corpo da primeira chamada carrega a chave de formato estruturado; o da segunda tem
      **apenas** as chaves de modelo, mensagens e temperatura.
    - Provedor devolvendo 400 citando o formato nas **duas** chamadas: levanta `LLMError`
      mencionando o código de status, tendo feito exatamente duas chamadas — o galho não vira
      laço (D-03).
    - Nenhum socket é aberto e nenhuma variável de ambiente é lida.
  </behavior>
  <action>
Dois testes novos em `test_smoke.py`, ambos offline. Nenhuma mudança em código de produção.

**Infraestrutura de duplo, compartilhada pelos dois testes.** Defina no módulo de teste (perto dos
dois testes, no estilo funcional do arquivo) duas classes pequenas:

- Uma resposta falsa, construída com status, texto e carga JSON, expondo os três atributos que
  `_chamar_provedor` consulta: `status_code`, `text` e um método `json()`.
- Um cliente falso que aceita qualquer argumento nomeado no construtor (o código real passa
  `timeout` e `headers`), implementa `__enter__` devolvendo a si mesmo e `__exit__` devolvendo
  falso, e cujo `post(url, json=None)` **registra o corpo recebido numa lista** e devolve a
  próxima resposta de um roteiro pré-carregado.

Instale o duplo com `monkeypatch.setattr(extractor.httpx, "Client", ClienteFalso)`, onde
`extractor` é `app.extractor` já importado no topo do arquivo de teste. O `monkeypatch` desfaz a
substituição ao fim de cada teste, então `app/fetcher.py` não é afetado. Instancie sempre
`LLMExtractor(api_key="chave-de-teste-sem-valor")`, para que a chave venha do construtor e nenhum
teste dependa de `LLM_API_KEY` no ambiente (D-12).

**(a) `test_degradacao_json_schema_faz_exatamente_uma_segunda_chamada(monkeypatch)`** — caminho
feliz do galho de D-02.

Roteiro de respostas: primeira com status 400 e um texto que cite o nome do campo de formato
estruturado usado pela condição de disparo (leia a linha exata de `_chamar_provedor` no
`read_first` e use o mesmo termo); segunda com status 200 e carga
`{"choices": [{"message": {"content": <json do briefing>}}]}`, onde o conteúdo é uma string JSON
de um briefing válido — basta `empresa` e `resumo`, que são os dois campos obrigatórios do schema.

Chame `extrair(url, titulo, texto)` e afirme: o retorno é um `Briefing` cujo `empresa` é o valor
que você colocou na carga; a lista de corpos registrados tem **exatamente 2** elementos; o
primeiro corpo **contém** a chave de formato estruturado; e o conjunto de chaves do segundo corpo
é **exatamente** `{"model", "messages", "temperature"}`. Essa igualdade exata de conjunto é o que
prova, de forma positiva, que a segunda chamada foi feita sem o formato estruturado.

Afirme também que as mensagens enviadas nas duas chamadas são **as mesmas**: D-02 registra que o
schema já viaja no prompt, então a segunda chamada não remonta as mensagens. Compare
`corpos[0]["messages"] == corpos[1]["messages"]`.

**(b) `test_segundo_400_de_json_schema_vira_llmerror(monkeypatch)`** — o galho não vira laço.

Roteiro com **duas** respostas de status 400 citando o mesmo termo. Use `pytest.raises(LLMError)`
em torno de `extrair(...)` e afirme: a mensagem da exceção menciona `400`; e a lista de corpos
registrados tem **exatamente 2** elementos. Esta é a asserção que trava D-03 — a segunda chamada
roda com o formato estruturado desligado, então a condição de disparo do galho não pode ser
satisfeita de novo, e não há terceira chamada.

Importe `LLMError` de `app.extractor` no topo do arquivo, junto dos símbolos já importados de lá.

**Registre no `SUMMARY` o que estes testes não provam:** eles travam a **lógica** do galho contra
um duplo. Não substituem verificação contra o provedor real — `gpt-oss-120b` não produz o 400 que
dispara o galho, limite já documentado em `COVERAGE.md`, no `05-05-SUMMARY.md` e em D-14, e
classificado pela `05-VERIFICATION.md` como corretamente caracterizado. Não altere `COVERAGE.md`.
  </action>
  <verify>
    <automated>pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest -q` sai com código 0 e reporta **16 testes passando** em menos de 3s.
    - `pytest -q -k "degradacao_json_schema or segundo_400"` sai com código 0 e reporta 2 testes
      passando.
    - Comportamento: com o roteiro 400-depois-200, `extrair` devolve um `Briefing` com o `empresa`
      da carga, e a lista de corpos registrados tem comprimento **exatamente 2**.
    - Comportamento: o conjunto de chaves do segundo corpo é exatamente
      `{"model", "messages", "temperature"}`, e `corpos[0]["messages"] == corpos[1]["messages"]`.
    - Comportamento: com o roteiro 400-depois-400, `extrair` levanta `LLMError` cuja mensagem
      contém `400`, e a lista de corpos registrados tem comprimento **exatamente 2**.
    - Nenhum teste abre socket: a suíte inteira roda com a interface de rede indisponível. Verifique
      que o duplo é instalado antes de qualquer chamada a `extrair` em ambos os testes.
    - `git diff --stat -- app/ requirements.txt .planning/phases/05-llm/COVERAGE.md` não imprime
      nada.
    - `python -c "import ast; t=ast.parse(open('test_smoke.py',encoding='utf-8-sig').read()); ns={n.name for n in t.body if isinstance(n,ast.FunctionDef)}; assert {'test_degradacao_json_schema_faz_exatamente_uma_segunda_chamada','test_segundo_400_de_json_schema_vira_llmerror'} <= ns; print('ok')"` imprime `ok` (`utf-8-sig` pelo BOM, ver Task 2).
  </acceptance_criteria>
  <reversibility rating="reversible">Testes aditivos em um arquivo; o duplo de `httpx.Client` é instalado via `monkeypatch` e desfeito ao fim de cada teste.</reversibility>
  <done>
    O galho de degradação de D-02 tem cobertura persistida: exatamente uma segunda chamada, feita
    sem o formato estruturado e com as mesmas mensagens, e um segundo 400 vira `LLMError` sem
    repetir. O limite de "não verificado contra provedor real" permanece registrado, intocado.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `briefings.db` -> processo da API | valor de `extrator` lido de linha antiga é dado não confiável na leitura, ainda que escrito por nós |
| site de terceiro -> prompt do modelo | conteúdo de página cruza para dentro da mensagem enviada ao provedor; é o vetor de injeção de prompt |
| processo -> provedor externo (Groq) | corpo da requisição e chave de API cruzam aqui; nesta task o limite é atravessado apenas por duplo |
| suíte de testes -> filesystem do projeto | um teste descuidado pode criar ou alterar `briefings.db` real |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-60 | Tampering | `BriefingResponse.extrator` | medium | mitigate | Task 1: `Literal` fecha a enumeração da SPEC §8; valor fora dela vindo de linha antiga vira miss pela guarda de 05-08 T2, em vez de atravessar a API como valor arbitrário lido pela UI |
| T-05-61 | Tampering | conteúdo de página de terceiro forjando o delimitador | high | mitigate | Task 2 (c): teste persistido trava a anti-forja de `DELIM_INICIO`/`DELIM_FIM` em `_montar_mensagens` (D-11, camada 2 de 3). Uma regressão passa a quebrar a suíte em vez de embarcar |
| T-05-62 | Denial of Service | laço de chamadas ao provedor no galho de degradação de D-02 | medium | mitigate | Task 3 (b): teste persistido prova exatamente duas chamadas e que um segundo 400 vira `LLMError` sem repetir (D-02, D-03). Fecha o modo de falha "segunda chamada vira alcançável mais de uma vez" |
| T-05-63 | Information Disclosure | chave de API dentro da suíte de testes | high | mitigate | Tasks 2 e 3 passam a chave por argumento de construtor com valor literal de teste e dublam `httpx.Client`; nenhum teste lê `LLM_API_KEY` do ambiente nem abre socket (D-12). Nenhuma chave real pode ser exercitada por acidente numa máquina com a variável exportada |
| T-05-64 | Tampering | `briefings.db` real tocado pela suíte | medium | mitigate | Task 2 monkeypatcha `db.DB_PATH` para `tmp_path` antes de qualquer chamada ao módulo; critério de aceite verifica por `git status --porcelain briefings.db`. Nenhuma DDL no banco real (D-10) |
| T-05-65 | Spoofing | modelo obedecendo instrução vinda do conteúdo da página | high | mitigate | Coberto pelas três camadas de D-11 já implementadas; a Task 2 (c) trava a camada de delimitador. As camadas 1 (mensagem de sistema) e 3 (validação de schema, L-03) permanecem verificadas pela `05-VERIFICATION.md` e não são reabertas aqui |

**Nível ASVS:** 1. **Bloqueio configurado:** `high`. As três ameaças `high` deste plano (T-05-61,
T-05-63, T-05-65) são todas `mitigate` e têm verificação automatizada — nenhum item `high` fica
pendente.

**Package legitimacy gate:** não se aplica. Nenhum pacote é instalado (L-06, D-01). Em particular,
os dublês de `httpx` são escritos à mão de propósito: `respx` e `pytest-mock` seriam pacotes novos,
e o projeto não instala dependência antes da apresentação. `requirements.txt` permanece intocado,
com critério de aceite explícito em cada task.
</threat_model>

<verification>
- `pytest -q` verde com **16 testes**, offline, sem chave de API e sem rede, em menos de 3s (D-12).
- `BriefingResponse.extrator` aceita exatamente `llm`, `heuristico` e `falha`; qualquer outro valor
  levanta `ValidationError`.
- Linha de cache com extrator fora da enumeração: **200**, `origem: novo` — não 500, não `cache`.
- `db.buscar` travado nas duas direções da regra de D-09, sobre banco em `tmp_path`.
- `_montar_mensagens` travado contra forja de delimitador em texto e título (D-11).
- Galho de degradação de D-02 travado: exatamente duas chamadas, segunda sem formato estruturado e
  com as mesmas mensagens; segundo 400 vira `LLMError` sem repetir (D-03).
- `git status --porcelain briefings.db` vazio; `git diff -- requirements.txt` vazio;
  `COVERAGE.md` intocado.
</verification>

<success_criteria>
- **WR-01 fechado:** a enumeração da SPEC §8 é exigível pelo Pydantic, e fechá-la não abriu rota de
  500 — a guarda do plano 05-08 converte a linha antiga em miss.
- **WR-02 fechado:** as três rotas que o `05-REVIEW.md` nomeia (D-09, D-02, D-11) têm teste
  persistido, offline, determinístico.
- A suíte permanece sem chave, sem rede e rápida; `briefings.db` real nunca é aberto por teste.
- Nenhuma dependência nova; nenhuma DDL; `COVERAGE.md` e os limites residuais já documentados
  permanecem exatos e intocados.

## Auditoria de fontes

A auditoria completa da batelada está em `05-08-PLAN.md`, seção
"Auditoria de fontes — batelada de fechamento de lacunas". Este plano cobre as linhas `WR-01` e
`WR-02` daquela tabela. **Nenhum item MISSING** no conjunto dos dois planos.

## Artifacts this phase produces

**Produzidos por este plano (05-09):**
- `app/schemas.py::BriefingResponse.extrator` — tipo fechado em
  `Literal["llm", "heuristico", "falha"]` (SPEC §8, WR-01)
- `test_smoke.py::test_cache_com_extrator_fora_da_enumeracao_vira_miss`
- `test_smoke.py::test_cache_heuristico_vira_miss_quando_llm_disponivel`
- `test_smoke.py::test_cache_llm_sobrevive_quando_llm_indisponivel`
- `test_smoke.py::test_montar_mensagens_remove_delimitador_forjado`
- `test_smoke.py::test_degradacao_json_schema_faz_exatamente_uma_segunda_chamada`
- `test_smoke.py::test_segundo_400_de_json_schema_vira_llmerror`
- Duplos de teste locais ao módulo, escritos à mão (sem pacote novo): uma resposta falsa com
  `status_code`/`text`/`json()` e um cliente falso com `__enter__`/`__exit__`/`post` que registra
  os corpos enviados

**Produzidos pelo plano 05-08 (roll-up da batelada):**
- `app/main.py::AVISO_CACHE_INDISPONIVEL`, `app/main.py::MSG_FALHA_GENERICA`
- `app/main.py::gerar_briefings` reestruturada (corpo por URL dentro de um `try`, braço
  `except Exception`, guarda de `db.salvar`, guarda de montagem da resposta de cache, import de
  `pydantic.ValidationError`)
- `app/main.py::_extrair_com_fallback` com o degrau 3 sem interpolação de exceção não autorada
- `test_smoke.py::test_falha_ao_salvar_no_cache_nao_derruba_o_lote`,
  `::test_cache_incompativel_com_o_schema_vira_miss`,
  `::test_falha_dupla_devolve_briefing_de_falha_sem_vazar_excecao`

**Nenhum arquivo novo é criado por esta batelada.** `requirements.txt`, `app/db.py`,
`app/extractor.py`, `app/config.py`, `app/main.py` (neste plano), `static/index.html`,
`.env.example` e `COVERAGE.md` permanecem intocados por 05-09.
</success_criteria>

<output>
Create `.planning/phases/05-llm/05-09-SUMMARY.md` when done
</output>
