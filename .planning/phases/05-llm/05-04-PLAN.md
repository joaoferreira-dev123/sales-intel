---
phase: 05-llm
plan: 04
type: execute
wave: 3
depends_on: ["05-03"]
files_modified:
  - app/config.py
  - .env.example
  - app/extractor.py
autonomous: true
requirements: [D-04, L-04, L-06]

estimate:
  tokens: 30000
  raw_tokens: 15000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "existe .env.example no repositorio, com LLM_API_KEY vazia, fechando o item de pronto da SPEC §17"
    - "nenhum segredo real esta versionado: .gitignore ignora .env e .env.example nao traz valor de chave"
    - "config.llm_api_key() le a variavel em tempo de chamada, entao monkeypatch.setenv continua funcionando nos testes 1 e 2"
    - "o corte de texto enviado ao modelo passa a vir de config.LLM_MAX_CHARS, com valor padrao 12000 (L-04)"
  artifacts:
    - "app/config.py::llm_api_key"
    - "app/config.py::LLM_MODELO"
    - "app/config.py::LLM_MAX_CHARS"
    - ".env.example"
  key_links:
    - "LLMExtractor.__init__ resolve api_key e modelo por app.config, nao por os.getenv inline"
    - "config.llm_api_key() e a unica fonte de verdade da chave, consumida tambem por /health (plano 06) e pela regra de cache (plano 07)"
---

<objective>
Criar `app/config.py` e `.env.example`, fechando parcialmente a divergencia da SPEC §13 e
integralmente o item "existe `.env.example`" da SPEC §17.

Por D-04 entram **apenas tres variaveis**: `LLM_API_KEY`, `LLM_MODELO`, `LLM_MAX_CHARS`.
`FETCH_TIMEOUT`, `CACHE_VALIDADE_DIAS`, `USER_AGENT`, `DATABASE_URL` e `DEBUG` permanecem
cravados no codigo nesta fase — pertencem a fases ja entregues e testadas, e move-las hoje e
superficie de regressao sem retorno de demonstracao.

Purpose: item 3 da tabela `<execution_order>` do CONTEXT.md. Se o tempo acabar aqui, o item de
"pronto" da §17 esta fechado e o `LLMExtractor` do plano 05 ja tem de onde ler configuracao.
Output: `app/config.py`, `.env.example`, e `LLMExtractor` lendo config em vez de `os.getenv`
inline.
</objective>

<planner_notes>
**Reversibility (D-04): `reversible`.** As outras cinco variaveis da SPEC §13 migram para
`config.py` depois sem tocar em chamador nenhum — o modulo e aditivo por construcao. Nao ha
decisao `one-way` neste plano, logo nenhum `checkpoint:decision` e necessario.

**Forma do modulo — decisao de projeto com motivo.** `llm_api_key()` e uma **funcao**, nao uma
constante de modulo, enquanto `LLM_MODELO` e `LLM_MAX_CHARS` sao constantes. A assimetria e
deliberada: os testes 1 e 2 (plano 03, ja no disco) trocam `LLM_API_KEY` por
`monkeypatch.setenv`/`delenv`, o que so funciona se a leitura acontecer em tempo de chamada. Uma
constante congelada no import quebraria os dois testes que travam L-05 — o teste mais importante
da fase. `LLM_MODELO` e `LLM_MAX_CHARS` nunca sao trocados em teste, entao seguem o padrao de
constante de modulo ja usado em `fetcher.py` (`TIMEOUT`, `MAX_BYTES`) e `db.py` (`VALIDADE`).
D-04 prescreve *quais* variaveis entram, nao a forma do modulo.

**Sem `LLM_BASE_URL` como variavel de ambiente.** D-04 trava o `config.py` em tres variaveis. O
endpoint do provedor vira constante de modulo em `app/extractor.py` no plano 05, o que mantem a
propriedade da SPEC §7 item 4 ("trocar de provedor mexe em um arquivo so") sem furar D-04.
</planner_notes>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@SPEC-sales-intel.md
@.planning/phases/05-llm/05-CONTEXT.md
@.planning/phases/05-llm/05-PATTERNS.md
@app/extractor.py
@app/fetcher.py
@.gitignore
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar app/config.py com as tres variaveis de D-04</name>
  <files>app/config.py</files>
  <read_first>
    - `app/extractor.py` linhas 1-19 e 76-78 — a docstring de modulo que estabelece o tom
      ("explica o porque, nao a implementacao") e o `os.getenv("LLM_API_KEY")` inline que este
      modulo passa a centralizar.
    - `app/fetcher.py` linhas 15-20 — modelo de constantes de modulo em UPPER_SNAKE_CASE com
      comentario de intencao.
    - `app/db.py` linhas 1-18 — segundo exemplo do mesmo padrao (docstring + constantes).
    - `SPEC-sales-intel.md` §13 (as oito variaveis) e §6 paragrafo "Seguranca" (chave de API so
      em variavel de ambiente, nunca no codigo).
    - `.planning/phases/05-llm/05-PATTERNS.md` secao "`app/config.py` (NOVO)".
  </read_first>
  <action>
Crie `app/config.py`, o primeiro modulo de configuracao do projeto.

**Docstring de modulo** em pt-BR, no mesmo tom de `app/db.py` e `app/extractor.py`: curta,
explicando a decisao e nao a implementacao. Precisa dizer duas coisas: (a) por D-04 apenas as
tres variaveis do LLM saem para o ambiente nesta fase, porque as demais pertencem a fases ja
entregues e mexer nelas agora e regressao sem retorno; (b) a chave e lida em tempo de chamada,
nao no import, para que o teste consiga troca-la sem recarregar o modulo.

**Conteudo, exatamente estes tres nomes publicos:**

- `def llm_api_key() -> str | None:` — devolve `os.getenv("LLM_API_KEY") or None`, de modo que
  string vazia e ausencia sejam tratadas igual (SPEC §13 documenta `LLM_API_KEY=` vazio como
  "cai no extrator heuristico"). Docstring de uma linha explicando o *porque* de ser funcao.
- `LLM_MODELO: str = os.getenv("LLM_MODELO", "gpt-4o-mini")` — o padrao e o valor da SPEC §13, o
  modelo mais barato que resolve (SPEC §6, custo e latencia).
- `LLM_MAX_CHARS: int = int(os.getenv("LLM_MAX_CHARS", "12000"))` — o corte de custo de L-04 e da
  SPEC §11. Comentario inline explicando que texto mais longo nao melhora o resultado e
  multiplica o preco por chamada.

Unico import: `import os`. Nao importe nada de `app` — este modulo fica na base da arvore de
dependencias e nao pode criar ciclo.

**Proibicoes:**
- Nao adicione `FETCH_TIMEOUT`, `CACHE_VALIDADE_DIAS`, `USER_AGENT`, `DATABASE_URL` nem `DEBUG`.
  Estao explicitamente fora de escopo por D-04 e listados nas ideias adiadas do CONTEXT.md.
- Nao adicione `LLM_BASE_URL` — o endpoint vira constante de modulo em `app/extractor.py` no
  plano 05.
- Nao escreva valor padrao para a chave. Sem chave, `llm_api_key()` devolve `None`.
- Nao adicione `print`, `logging` nem qualquer coisa que possa ecoar o valor da chave.
  </action>
  <verify>
    <automated>python -c "import os; os.environ.pop('LLM_API_KEY', None); import app.config as c; assert c.llm_api_key() is None; assert c.LLM_MODELO == 'gpt-4o-mini'; assert c.LLM_MAX_CHARS == 12000; os.environ['LLM_API_KEY']='x'; assert c.llm_api_key() == 'x'; os.environ['LLM_API_KEY']=''; assert c.llm_api_key() is None; print('ok')" && pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - `app/config.py` existe e importa sem efeito colateral.
    - `python -c "import app.config as c; print(sorted(n for n in vars(c) if not n.startswith('_')))"` lista exatamente `LLM_MAX_CHARS`, `LLM_MODELO`, `llm_api_key` e `os`.
    - Leitura em tempo de chamada comprovada: alterar `os.environ["LLM_API_KEY"]` **depois** do
      import muda o retorno de `c.llm_api_key()`.
    - `LLM_API_KEY` vazio (`""`) resulta em `llm_api_key() is None`.
    - `grep -v '^\s*#' app/config.py | grep -cE 'FETCH_TIMEOUT|CACHE_VALIDADE_DIAS|USER_AGENT|DATABASE_URL|DEBUG|LLM_BASE_URL' || true` imprime `0`.
    - `grep -v '^\s*#' app/config.py | grep -cE 'print\(|logging' || true` imprime `0`.
    - `pytest -q` sai com codigo 0 e reporta 6 testes.
  </acceptance_criteria>
  <done>
    `app/config.py` centraliza as tres variaveis de D-04, com a chave lida em tempo de chamada.
  </done>
  <reversibility rating="reversible">
    D-04 e reversivel: as outras cinco variaveis da SPEC §13 migram para este modulo depois sem
    tocar em nenhum chamador, porque `config` e importado, nao injetado.
  </reversibility>
</task>

<task type="auto">
  <name>Task 2: Criar .env.example sem nenhum segredo</name>
  <files>.env.example</files>
  <read_first>
    - `.gitignore` — confirme que `.env` ja esta listado (esta, na linha 4). `.env.example`
      **deve** ser versionado; `.env` **nao**.
    - `SPEC-sales-intel.md` §13 (bloco de variaveis, com os nomes e valores de exemplo) e §17
      ("Nenhum segredo no repositorio; existe `.env.example`").
    - `app/config.py` — recem-criado; os tres nomes precisam bater exatamente.
  </read_first>
  <action>
Crie `.env.example` na raiz do repositorio, formato dotenv (`CHAVE=valor`, uma por linha, sem
aspas), contendo **somente** as tres variaveis de D-04, na mesma ordem em que aparecem na SPEC
§13:

- `LLM_API_KEY=` — **sem valor**. Acima dela, um comentario de uma linha (`#`) em pt-BR dizendo
  que vazio significa cair no extrator heuristico, exatamente como a SPEC §13 documenta.
- `LLM_MODELO=gpt-4o-mini`
- `LLM_MAX_CHARS=12000`

Abra o arquivo com um comentario de duas linhas explicando que este e o arquivo de exemplo, que
o arquivo real se chama `.env`, que `.env` esta no `.gitignore` e que nenhum valor real de chave
pode ser escrito aqui.

**Proibicoes duras:**
- Nenhum valor para `LLM_API_KEY`, nem um placeholder que pareca uma credencial (nada como
  `sk-...`, `xxx-real-key`, nem chave truncada).
- Nenhuma das outras cinco variaveis da SPEC §13 — elas continuam cravadas no codigo por D-04.
- Se `.gitignore` por algum motivo nao contiver `.env`, acrescente a linha; se ja contiver, nao
  toque no arquivo.
  </action>
  <verify>
    <automated>python -c "import io,re; t=io.open('.env.example',encoding='utf-8').read(); linhas=[l for l in t.splitlines() if l.strip() and not l.strip().startswith('#')]; assert linhas == ['LLM_API_KEY=', 'LLM_MODELO=gpt-4o-mini', 'LLM_MAX_CHARS=12000'], linhas; assert not re.search(r'sk-[A-Za-z0-9]', t); print('ok')" && python -c "import io; g=io.open('.gitignore',encoding='utf-8').read().splitlines(); assert '.env' in [l.strip() for l in g]; print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `.env.example` existe na raiz e contem exatamente tres linhas nao comentadas:
      `LLM_API_KEY=`, `LLM_MODELO=gpt-4o-mini`, `LLM_MAX_CHARS=12000`.
    - A linha `LLM_API_KEY=` nao tem nada depois do sinal de igual.
    - O arquivo nao casa com o padrao `sk-[A-Za-z0-9]` nem com nenhuma outra forma de credencial.
    - `.gitignore` contem a linha `.env`.
    - `git check-ignore -q .env.example` retorna codigo diferente de 0 (ou seja, o exemplo **e**
      versionado) e `git check-ignore -q .env` retorna 0.
    - Os tres nomes batem exatamente com os nomes lidos por `app/config.py`.
  </acceptance_criteria>
  <done>
    O item "existe `.env.example`" da SPEC §17 esta fechado, sem nenhum segredo no repositorio.
  </done>
</task>

<task type="auto">
  <name>Task 3: LLMExtractor passa a ler configuracao de app.config</name>
  <files>app/extractor.py</files>
  <read_first>
    - `app/extractor.py` — arquivo inteiro. Alvos: o bloco de imports (linhas 21-25),
      `LLMExtractor.__init__` (linhas 76-78, com `os.getenv("LLM_API_KEY")` e o default
      `modelo: str = "gpt-4o-mini"`), `disponivel()` (linhas 80-81) e a linha do corte
      `_trecho = texto[:12000]` (linha 88).
    - `app/config.py` — recem-criado, para os nomes exatos.
    - `test_smoke.py` — testes 1 e 2 (plano 03) monkeypatcham a **variavel de ambiente**; esta
      task nao pode quebra-los.
    - `.planning/phases/05-llm/05-PATTERNS.md` secao "`app/extractor.py` — `LLMExtractor.extrair()`".
  </read_first>
  <action>
Em `app/extractor.py`, troque a leitura de ambiente inline por `app.config`.

- **Imports:** acrescente `from . import config` ao bloco de imports locais, junto de
  `from .schemas import Briefing`. Remova `import os` **se e somente se** nenhuma outra
  referencia a `os` sobrar no arquivo.
- **`LLMExtractor.__init__`:** a assinatura passa a
  `def __init__(self, api_key: str | None = None, modelo: str | None = None):`. No corpo:
  `self.api_key = api_key or config.llm_api_key()` e `self.modelo = modelo or config.LLM_MODELO`.
  O default de modelo deixa de ser um literal na assinatura e passa a vir da configuracao.
- **`disponivel()`:** permanece `return bool(self.api_key)`, inalterado.
- **Corte de texto (L-04):** a linha `_trecho = texto[:12000]` passa a
  `_trecho = texto[: config.LLM_MAX_CHARS]`. Mantenha o comentario de custo que ja esta acima
  dela.
- **`extrair()` continua terminando no erro de nao implementado nesta wave.** O corpo do metodo
  so e completado no plano 05. Nao antecipe a chamada HTTP aqui.
  <!-- planner-discipline-allow: NotImplementedError -->
- **`escolher_extrator()`:** permanece exatamente como esta. Ele instancia `LLMExtractor()` sem
  argumentos e pergunta `disponivel()`; com a resolucao via `config.llm_api_key()` em tempo de
  chamada, os testes 1 e 2 continuam valendo sem alteracao.

Nenhum pacote novo entra no `requirements.txt` (L-06).
  </action>
  <verify>
    <automated>pytest -q && python -c "import os; os.environ.pop('LLM_API_KEY', None); import app.extractor as e; x=e.LLMExtractor(); assert x.api_key is None and x.modelo=='gpt-4o-mini'; os.environ['LLM_API_KEY']='k'; assert e.LLMExtractor().disponivel() is True; assert isinstance(e.escolher_extrator(), e.LLMExtractor); print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `pytest -q` sai com codigo 0 e reporta 6 testes — em especial os dois testes
      `escolher_extrator` do plano 03 continuam verdes **sem edicao**.
    - `grep -v '^\s*#' app/extractor.py | grep -c 'os.getenv' || true` imprime `0`.
    - `grep -v '^\s*#' app/extractor.py | grep -c '12000' || true` imprime `0`.
    - `python -c "import app.extractor as e; import inspect; s=inspect.signature(e.LLMExtractor.__init__); assert str(s.parameters['modelo'].default) == 'None'; print('ok')"` imprime `ok`.
    - Resolucao em tempo de chamada comprovada: definir `os.environ["LLM_API_KEY"]` depois do
      import faz `LLMExtractor().disponivel()` virar `True` e `escolher_extrator()` devolver um
      `LLMExtractor`.
    - `git diff --stat requirements.txt` nao mostra alteracao (L-06).
  </acceptance_criteria>
  <done>
    A configuracao do LLM tem uma unica fonte de verdade (`app/config.py`), e o corte de 12k de
    L-04 passa a ser configuravel sem quebrar nenhum teste.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ambiente do processo -> codigo da aplicacao | `LLM_API_KEY` entra por variavel de ambiente e nunca deve sair de la |
| repositorio git -> mundo | `.env.example` e versionado; `.env` nao pode ser |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-15 | Information Disclosure | `.env.example` versionado | high | mitigate | Task 2 exige `LLM_API_KEY=` sem valor; criterio de aceite rejeita qualquer padrao de credencial e verifica que `.gitignore` cobre `.env` (SPEC §6, §17) |
| T-05-16 | Information Disclosure | `app/config.py` ecoando o segredo | high | mitigate | Task 1 proibe `print` e `logging` no modulo; criterio de aceite faz grep negativo; a superficie publica e apenas `llm_api_key()` |
| T-05-17 | Information Disclosure | segredo em mensagem de erro do extrator | high | mitigate | `__init__` apenas armazena; nenhuma mensagem de excecao deste plano interpola `self.api_key`. O endurecimento completo da chamada HTTP vem no plano 05 (T-05-19) |
| T-05-18 | Tampering | dependencia nova entrando antes da demo | medium | mitigate | nenhum `pip install` neste plano; `requirements.txt` intocado (L-06, D-01). Package-legitimacy gate nao se aplica: nao ha task de instalacao de pacote nesta fase |
| T-05-19 | Denial of Service | `LLM_MAX_CHARS` mal configurado inflando custo/latencia | low | accept | valor padrao 12000 conforme SPEC §11; operador que aumentar assume o custo; quota por usuario depende da autenticacao da Fase 6 |

</threat_model>

<verification>
- `pytest -q` verde com 6 testes, sem editar os testes do plano 03.
- `app/config.py` expoe exatamente tres nomes publicos.
- `.env.example` versionado, sem valor de chave; `.env` ignorado.
</verification>

<success_criteria>
- D-04 cumprido: tres variaveis, nem mais nem menos.
- Item de "pronto" da SPEC §17 fechado (existe `.env.example`, nenhum segredo no repositorio).
- L-04 (corte de 12k) passa a vir de configuracao, com o mesmo valor padrao.
- L-06 respeitado: nenhuma dependencia nova.
</success_criteria>

## Artifacts this phase produces

**Produzidos por este plano:**
- `app/config.py` (novo): `llm_api_key() -> str | None`, `LLM_MODELO: str`, `LLM_MAX_CHARS: int`
- `.env.example` (novo): `LLM_API_KEY=`, `LLM_MODELO=gpt-4o-mini`, `LLM_MAX_CHARS=12000`
- `app/extractor.py::LLMExtractor.__init__` com assinatura `(api_key=None, modelo=None)`

**Demais artefatos da fase (roll-up completo):**
- Plano 01: `app/main.py::_extrair_com_fallback`, `::BASE_DIR`, `::STATIC_DIR`.
- Plano 02: `app/fetcher.py::ROBOTS_TIMEOUT`; `pode_raspar()` reescrito; `static/index.html` em
  UTF-8 sem BOM.
- Plano 03: os tres testes de fallback em `test_smoke.py`.
- Plano 05: `app/extractor.py::LLMError`, `::LLM_BASE_URL`, `::LLM_TIMEOUT`,
  `::LLMExtractor._montar_mensagens`, `::LLMExtractor._chamar_provedor`,
  `::LLMExtractor.extrair`; `test_smoke.py::test_llm_sem_chave_levanta_erro_claro`.
- Plano 06: `app/schemas.py::BriefingResponse.degradado`; `/health` com `llm_disponivel`;
  `static/index.html` classe `.tag-aviso` e `<span>` condicional em `render()`.
- Plano 07: `app/db.py::buscar(url, llm_disponivel)` com a regra de upgrade de extrator.

<output>
Create `.planning/phases/05-llm/05-04-SUMMARY.md` when done
</output>
